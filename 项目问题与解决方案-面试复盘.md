# LinkNest 联巢商城项目问题与解决方案（面试复盘）

> 用途：回答面试官“项目中遇到过什么问题、怎么定位、怎么解决、如何证明”的追问。  
> 项目范围：`ekko-pay-mall`、`ekko-group-buy-market`、`ekko-group-buy-web`、`ekko-group-chat`。  
> 首次整理：2026-08-14。  
> 维护原则：以后本项目出现并处理的新问题，继续追加到本文，不另建零散问题文档。

## 1. 面试时先说明证据边界

本文把结论分成三种状态：

- **已解决并有测试证据**：源码已经实现，并完成对应单元测试、构建或样本回放。
- **已实现但未完成真实联调**：源码和单元测试已完成，但没有真实 MySQL、Redis、RabbitMQ、支付宝、微信、OSS、MinerU 或 Elasticsearch 的完整联调证据。
- **遗留风险**：已经识别，但当前版本仍需要继续完善，面试时不能说成已经解决。

面试中不要只说“测试通过”。应明确测试证明了什么：

```text
单元测试通过
  != 跨服务 HTTP 成功
  != Redis/RabbitMQ 可用
  != 支付宝真实退款成功
  != MinerU、OSS、ES、模型端到端联通
```

推荐表述：

> 我会把验证分成源码、单元测试、HTTP、数据库、缓存/MQ和第三方平台几个层级。当前哪些层级已经验证、哪些还没有，我会明确说明，不用 HTTP 200 或 Maven BUILD SUCCESS 代替端到端成功。

## 2. 项目总体链路

```text
Vue 商城
  ├─ 微信扫码登录 → 商城 JWT
  ├─ 商品/订单 → ekko-pay-mall
  │                  ├─ 创建商城订单
  │                  ├─ 调用拼团服务锁单/结算/退单
  │                  ├─ 支付宝支付/退款
  │                  └─ RabbitMQ 消费退款消息
  ├─ 拼团营销 → ekko-group-buy-market
  │                  ├─ 活动试算与人群规则
  │                  ├─ Redis + DB 防超卖
  │                  ├─ 团和成员状态机
  │                  └─ notify_task 可靠通知
  └─ 智能客服 → ekko-group-chat
                     ├─ OSS + MinerU 异步解析
                     ├─ Markdown 预处理与父子分片
                     ├─ Embedding + Elasticsearch
                     └─ LangChain4j RAG 回答与引用
```

## 3. 问题速查表

| 编号 | 问题 | 核心解决方案 | 当前状态 |
|---|---|---|---|
| P01 | 商城与拼团订单无法可靠关联 | 统一使用商城 `orderId/outTradeNo`，保存营销价格快照 | 已实现，真实跨服务联调仍需补证 |
| P02 | 高并发参团可能超卖 | Redis 预占 + DB 条件更新双层兜底 | 已实现 |
| P03 | 支付宝重复回调可能重复结算 | 验签 + 订单条件更新 + 状态回查 | 已实现，真实支付宝联调未验证 |
| P04 | 支付已成功但拼团结算 HTTP 失败 | 先落 MySQL 支付事实，再由定时任务补偿结算 | 已实现，真实联调未验证 |
| P05 | 重复结算导致人数重复累加/重复成团 | 成员条件更新，只有首次转换增加 `complete_count` | 已实现 |
| P06 | 成团通知可能丢失或重复 | `notify_task` 本地消息表、条件抢占、重试和唯一业务号 | 已实现，部分基础设施联调未验证 |
| P07 | 退款跨商城、拼团、MQ、支付宝，容易状态不一致 | `WAIT_REFUND` 中间态、稳定退款号、查询后重试、补偿任务 | 已实现，真实资金联调未验证 |
| P08 | 请求体 `userId` 可被伪造查询他人订单 | JWT subject 是权威身份，body 只做一致性校验 | 已实现 |
| P09 | 微信二维码 ticket 可能被其他浏览器轮询 | `sceneStr ↔ ticket` 双向绑定 | 已实现；多实例缓存仍需迁 Redis |
| P10 | ticket、openid、JWT 生命周期混淆 | openid 做稳定用户标识，ticket/JWT 只做短期凭证 | 已实现 |
| P11 | 前端显示已登录，但后端返回 401/1006 | 校验 JWT 过期；认证失败统一清理会话并重新登录 | 已解决并有前端测试历史证据 |
| P12 | 新用户没有历史标签，是否无法参团 | 用稳定 userId 绑定标签，并设计新客/默认人群兜底 | 规则边界已明确，运营策略仍需配置 |
| P13 | 商品、SKU、活动、折扣表职责混乱 | 配置表预置，交易表由运行时写入，明确跨表关联 | 已梳理；真实 DDL 仍需核验 |
| P14 | Java 实体/Mapper 被误当成正式建表依据 | 当前 DDL、历史脚本和线上数据库证据分层 | 已形成规范 |
| P15 | MinerU 同步解析会阻塞上传且难恢复 | OSS 私有存储 + MinerU 异步任务 + MySQL 状态机 | 已实现，真实联调未验证 |
| P16 | 文档分片残留 OCR 异体字、HTML 表格和孤立标题 | NFKC/OCR、表格键值化、小块合并和质量校验 | 已解决，客服模块测试通过 |
| P17 | 按字符切分把表格行和标签截断 | 改为 Markdown 块/完整行切分，重叠只复用完整块 | 已解决 |
| P18 | 分片缺少产品身份导致跨型号误召回 | Embedding 加型号/标题/章节，查询型号转 ES metadata 过滤 | 已解决 |
| P19 | 小分片召回准但回答上下文不足 | 子分片检索、父分片回填 | 已实现 |
| P20 | 修改预处理规则后旧索引无法批量升级 | 保存处理版本并提供重新索引接口 | 已解决，数据库迁移需执行 |
| P21 | 动态配置修改后各实例不一致 | Redis Bucket 保存值、Topic 广播、反射更新本地字段 | 已实现；服务鉴权仍是风险 |
| P22 | IDEA 已停止但端口仍被 Java 占用 | `lsof` 定位监听进程、核对 cwd/命令/父进程后再终止 | 已解决过，属于排障流程 |
| P23 | Maven 报“无效的目标发行版 21” | 检查 Maven JVM，绕过 `.mavenrc` 的 Java 8 | 已解决过，属于环境问题 |
| P24 | Mockito 在 JDK 21 下无法自挂载 | 通过 Surefire `-javaagent` 显式加载 Mockito Agent | 已解决 |
| P25 | 管理登录页视觉右偏和移动端溢出 | 单卡片居中布局，并用浏览器几何与多尺寸验证 | 已解决 |
| P26 | MinerU Markdown 引用图片但系统只保存 `full.md` | 解包保存图片、视觉描述、稳定 imageId、私有 OSS 临时签名和分片关联 | 已实现并通过单测；真实 MinerU/OSS/视觉模型待联调 |
| P27 | 长文档向量化超过模型服务单批 10 条限制 | 最多 10 条一批串行 Embedding，全部成功后再替换 ES 旧索引 | 已解决并通过单测；真实模型重试待验证 |
| P28 | Embedding 从 1024 升到 1536 后空索引仍可能维度冲突 | 使用全新版本化索引，显式创建/校验 mapping，并校验模型返回维度 | 已实现并通过单测；真实 ES 和模型重建待验证 |
| P29 | 上传文件只按扩展名或客户端 MIME 路由，可能误解析伪装文件 | Apache Tika 内容检测、扩展名兼容校验和 OOXML 核心条目检查 | 已解决并通过单测；真实 OSS/MinerU 上传待验证 |
| P30 | 悬浮客服只有一条消息流，长时间使用后不同问题混在一起 | 保留快捷悬浮框，新增独立客服工作台和浏览器端多会话隔离 | 已实现并通过前端测试、构建和桌面/移动浏览器验证 |
| P31 | 商城像品牌展示页、交易信息不突出，客服页卡片装饰干扰阅读 | 商城采用高密度拼购信息层级，客服采用中性侧栏与居中消息流，并逐断点做浏览器验证 | 已解决并通过前端测试、构建和桌面/移动浏览器验证 |
| P32 | 商城改为红色拼购风格后，悬浮客服仍是荧光绿和米色网格 | 收敛为商城红单一强调色，复用独立客服的中性消息流并缩小面板 | 已解决并通过前端测试、构建和桌面/移动浏览器验证 |
| P33 | 商城直接展示“服务端返回、状态同步、履约”等开发术语 | 按消费者购买旅程重写首页、下单、订单和客服文案，技术术语只保留在内部页面与文档 | 已解决并通过前端测试、构建和桌面/移动浏览器验证 |
| P34 | 首页大标题字号失衡，主图占比过大且图片背景形成明显矩形色块 | 保留原文案，调整标题比例；生成并校验真实 Alpha 产品切图，移除蒙版和混合模式 | 已解决并通过前端测试、构建和桌面/移动浏览器验证 |
| P35 | 顶部促销栏与主导航重复展示商城名称并占用首屏高度 | 删除固定促销栏及响应式样式，让主导航在桌面和手机端统一贴顶 | 已解决并通过前端测试、构建和桌面/移动浏览器验证 |
| P36 | 生产密钥硬编码并被打入 JAR | 环境变量注入、制品排除本地 profile、服务器密钥文件限权 | 制品与服务器实启已验证，已暴露旧密钥仍需轮换 |
| P37 | Maven 生产 Profile 不生效且 2C4G 参数过大 | 运行时选择环境，按共享 2C4G 主机重做 JVM、连接池和容器预算 | 已上线运行，容量上限仍需压测 |
| P38 | 生产首启出现 Redis 鉴权、支付条件 Bean 和 ES 健康检查错配 | 将配置真正传入客户端，用条件 Bean 切断未启用能力，统一 ES 连接地址 | 已修复并通过服务器实启验证 |
| P39 | 客服模型已配置但同步接口角色不生效且响应超时 | 为同步 RAG/通用问答拆分模型 Bean，并关闭 Qwen 默认深度思考 | 已修复并通过生产 HTTP 验证；知识索引仍为空 |
| P40 | 真实资料入库被 MinerU 大文件、图片上限和 ES 型号分词阻断 | 压缩上传副本、外部化图片上限、型号 `.keyword` 精确过滤 | 已修复并完成全量入库与生产问答 E2E |
| P41 | 客服后端已有 SSE，但前端仍等待同步完整回答 | `fetch + ReadableStream` 解析事件，逐段更新进度、引用和回答 | 已修复、上线并通过公网浏览器验证 |
| P42 | 登录成功后状态提示不明显，账号窗口仍容易回到登录表单 | 统一读写 JWT 会话，打开前重新同步，新增“已登录”导航和账号概览 | 已修复、上线；前端 19 项测试与构建通过 |
| P43 | 客服长回答被页面裁切且 Markdown 标记直接显示 | 修正独立滚动容器高度链，使用禁用原始 HTML 的 Markdown 渲染组件 | 已修复、上线；前端 22 项测试与公网长回答验证通过 |
| P44 | 支付未配置时先完成拼团锁单，随后下单失败并遗留名额占用 | 前移支付能力校验，并固定按上海时区解析支付宝回调时间 | 已修复上线；沙箱付款、验签、落库和拼团结算 E2E 通过 |
| P45 | 客服推荐问题回答质量不一，正文反复出现“资料1”影响阅读 | 用真实生产问答筛选推荐问题，提示词取消行内资料编号，前端兼容清理历史引用标记 | 已修复上线；前端 25 项、客服后端 61 项测试及公网问答验证通过 |

## 4. 业务与分布式一致性问题

### P01 商城订单与拼团订单怎样可靠关联

**问题现象**

商城先创建本地订单，随后调用拼团服务锁单。如果两边各自生成订单号，会造成支付、营销订单、团和成员明细无法稳定关联；失败重试还可能重复创建营销订单。

**根因**

跨服务缺少统一业务幂等键，而且商品原价、优惠金额和实际支付金额分属两个服务。

**解决方案**

- 商城先生成并保存订单，状态为 `CREATE`。
- 调用 `lock_market_pay_order` 时沿用同一个 `orderId/outTradeNo`。
- 拼团服务返回 `originalPrice`、`deductionPrice`、`payPrice` 和 `teamId`。
- 商城把营销金额和队伍信息作为订单快照保存，并用 `payPrice` 创建支付宝支付单。
- 重试时根据同一业务订单号读取已有记录，不重新生成第二套订单关系。

**验证重点**

不能只看 HTTP 返回。还要核对商城 `pay_order`、拼团 `group_buy_order_list`、活动/队伍 ID、金额快照以及 Redis 名额。

**面试回答**

> 我们没有让商城订单和拼团订单各自生成一套无法关联的编号，而是用商城订单号作为跨服务业务幂等键。拼团服务负责计算营销价格，商城保存价格快照并负责支付，因此后续结算、退款和对账都能沿同一个订单号追踪。

### P02 并发参团怎样避免超卖

**问题现象**

多个用户同时加入最后几个名额时，如果先查库存再更新，线程之间会读到相同剩余数量，导致团成员超过目标人数。

**根因**

“查询剩余名额”和“占用名额”不是原子操作，只依靠 JVM 锁也无法覆盖多实例。

**解决方案**

- Redis 侧执行原子预占，快速拦截已满场景。
- 数据库侧用带条件的更新兜底，例如 `lock_count < target_count`。
- 只有条件更新成功才继续创建成员明细。
- 出现事务异常时由数据库事务回滚；Redis 与数据库之间的跨资源一致性通过后续释放/补偿处理。

**证据位置**

- `TradeRepository.occupyTeamStock()`
- `group_buy_order_mapper.xml`

**面试回答**

> 防超卖用了两层控制：Redis 做高性能原子预占，数据库条件更新做最终兜底。这样既减少数据库竞争，又避免仅依赖缓存造成最终人数越界。

### P03 支付宝重复回调怎样保证幂等

**问题现象**

支付宝可能重复通知。如果每次回调都更新订单、发布事件和调用拼团结算，会造成重复增加完成人数或重复通知。

**根因**

第三方回调天然是“至少一次”，不能假设只调用一次。

**解决方案**

1. 先校验支付宝签名。
2. 只接受明确的 `TRADE_SUCCESS`，并解析 `gmt_payment`。
3. 数据库只允许 `PAY_WAIT → PAY_SUCCESS` 条件更新。
4. 影响行数为 0 时回查当前状态；只有已经支付或后续状态才视为幂等成功。
5. 只有首次状态转换才发布普通订单支付事件或调用拼团结算。

**面试回答**

> 幂等不是简单加 Redis 锁，而是把数据库状态机作为最终判断。回调只能把 `PAY_WAIT` 改成 `PAY_SUCCESS`，重复回调影响 0 行后再回查状态，不重复执行下游副作用。

### P04 支付成功后，远程拼团结算失败怎么办

**问题现象**

用户已完成支付，但商城调用拼团结算接口时可能超时或网络失败。如果把整个流程当成一个同步事务，既无法回滚支付宝，也可能让本地支付状态丢失。

**根因**

MySQL 本地事务无法覆盖远程 HTTP 和第三方支付，跨服务不存在一个真正的数据库事务。

**解决方案**

- 先把 `PAY_SUCCESS` 和 `pay_time` 可靠写入商城 MySQL，MySQL 是支付事实来源。
- 再调用拼团结算，失败不回滚已经确认的支付事实。
- `MarketPaySettlementJob` 定时扫描已支付但未确认营销结算的订单并重试。
- Redis 的 30 天确认标记只减少无意义重试，不能替代 MySQL 支付状态。

**面试回答**

> 我们采用“本地事实优先、远程副作用可补偿”的方式。支付回调先可靠落库，再调用拼团结算；远程失败由定时任务重试，避免为了追求伪分布式事务而丢失真实支付结果。

### P05 重复结算怎样避免人数重复累加

**问题现象**

商城补偿任务和支付回调可能同时或重复调用结算接口。如果每次都执行 `complete_count + 1`，会导致提前成团和重复通知。

**解决方案**

- 成员明细只允许从未完成状态 `0 → 1` 条件更新。
- 更新影响 0 行时，回查成员是否已经完成；已经完成则作为幂等成功。
- 只有首次完成成员状态转换的请求才能增加队伍 `complete_count`。
- 队伍更新继续带 `complete_count < target_count/lock_count` 条件。
- 最终成团状态也通过条件更新保证只转换一次。

**面试回答**

> 结算幂等落在成员和队伍两级状态机上。先保证某个成员只完成一次，再保证队伍计数和成团状态只推进一次，因此重复 HTTP 请求不会产生重复人数和通知。

### P06 成团通知怎样避免丢失和重复

**问题现象**

如果在数据库事务里直接发送 HTTP 或 MQ，数据库提交后发送失败会丢消息；发送成功但本地更新失败又会重复发送。

**根因**

数据库事务与 HTTP/MQ 不属于同一原子事务。

**解决方案**

- 在业务事务内创建 `notify_task` 本地消息记录。
- 使用稳定 `uuid` 作为业务幂等标识，并依赖唯一约束防止重复任务。
- 定时任务只扫描待执行/待重试记录。
- 发送前通过数据库条件更新把任务抢占为“处理中”，避免多线程重复发送。
- 成功、待重试、终止失败分别更新状态，并记录 `last_error`。
- HTTP 必须同时检查状态码和业务响应；MQ 发送异常向上抛出，触发重试。

**面试回答**

> 我们用本地消息表解决事务外副作用。业务状态和通知任务在一个数据库事务里提交，发送由后台任务负责；通过唯一业务号和数据库条件抢占处理重复发送与多实例竞争。

### P07 跨系统退款怎样保证最终一致性

**问题现象**

退款同时涉及商城订单、拼团成员和队伍、RabbitMQ、支付宝。任意一步失败都可能出现“拼团已退但支付宝未退款”或“支付宝已退款但本地仍显示处理中”。

**解决方案**

- 未支付订单在营销退单成功后直接进入 `CLOSE`。
- 已支付订单先完成营销退单，再进入 `WAIT_REFUND`，不提前标记退款完成。
- 拼团服务在事务内写退款 `notify_task`，通过 `topic.team_refund` 发布消息。
- 商城使用独立退款队列，配置单条消费、重试和死信队列。
- 退款金额只取本地 `pay_order.pay_amount` 快照，不信任消息传入金额。
- 使用稳定退款号 `REFUND_<outTradeNo>`，重复请求仍对应同一退款业务。
- 每次退款前先查询支付宝；只有 `fund_change=Y` 或查询到 `REFUND_SUCCESS`，才允许 `WAIT_REFUND → CLOSE`。
- `RefundCompensationJob` 扫描长时间停留在 `WAIT_REFUND` 的订单，继续查询或使用相同退款号重试。

**验证边界**

源码和单元测试不能证明真实资金已经退回，仍需支付宝沙箱、MQ、两库状态和对账证据。

**面试回答**

> 退款没有使用一个长同步事务，而是引入 `WAIT_REFUND` 中间态。拼团退单成功只代表营销资源已释放，只有支付宝明确发生资金变化或查询到退款成功，商城订单才关闭；不确定状态由补偿任务继续查询和重试。

### P08 如何防止用户查询或退款他人订单

**问题现象**

如果后端直接使用请求体中的 `userId`，调用者可以替换成其他用户 ID 查询订单或申请退款。

**解决方案**

- 受保护接口从 `Authorization: Bearer <JWT>` 解析用户身份。
- JWT subject 是权威 `userId`。
- 请求体中的 `userId` 只做一致性校验，不作为授权来源。
- 查询 SQL 必须带 `user_id` 条件。
- 订单列表使用 `id < lastId ORDER BY id DESC LIMIT pageSize + 1` 游标分页，避免深分页性能问题。

**面试回答**

> 用户身份只能来自服务端验证过的 JWT，body 中的 userId 不可信。订单查询和退款都用 JWT subject 限定数据归属，同时使用游标分页避免数据增长后的深分页问题。

## 5. 微信登录、用户身份与前端会话问题

### P09 微信二维码被其他浏览器轮询的问题

**问题现象**

旧实现只保存 `ticket → openid`，浏览器轮询时只传 ticket。只要 ticket 泄露或被其他页面拿到，就可能绕过当前浏览器场景完成登录。

**根因**

二维码没有与发起登录的浏览器场景绑定；固定数字 scene 也无法区分不同登录会话。

**解决方案**

- 前端为每次登录生成 `sceneStr`。
- 后端使用微信 `QR_STR_SCENE/scene_str` 创建约 300 秒有效的二维码。
- 缓存 `sceneStr → ticket` 和 `ticket → sceneStr` 两个方向。
- 轮询时同时提交 ticket 和 sceneStr，双向都匹配才读取 `ticket → openid` 并签发 JWT。
- 新接口生成的 ticket 不能通过旧版只传 ticket 的接口绕过校验。
- 微信回调只为 `SCAN/subscribe` 二维码事件保存扫码结果。

**遗留风险**

当前绑定数据使用 Guava 进程内缓存，多实例部署会不一致，应迁移到 Redis，并配置合理 TTL。

**面试回答**

> ticket 是一次二维码凭证，不是用户 ID。我给二维码增加了浏览器 scene，并做 scene 和 ticket 双向绑定；只有发起二维码的页面携带正确组合才能换取登录 JWT，防止串号和旧接口绕过。

### P10 ticket、sceneStr、openid、JWT 分别解决什么问题

| 对象 | 稳定性 | 作用 |
|---|---|---|
| `sceneStr` | 每次页面登录会话不同 | 标识发起二维码的浏览器场景 |
| `ticket` | 每次申请二维码不同、短期有效 | 微信二维码凭证 |
| `openid` | 同一公众号 AppID 下同一用户通常稳定 | 商城业务用户标识，写入 JWT subject 和订单 `user_id` |
| JWT | 每次登录重新签发、短期有效 | 后续接口的登录凭证 |

不同 ticket 可以最终对应同一个 openid，因为同一用户可以多次扫码。人群标签和历史订单应绑定稳定用户标识 openid/userId，而不是 ticket 或 JWT 字符串。

当前没有独立用户表也能通过 `pay_order.user_id = JWT subject/openid` 查询历史订单；如果后续要保存昵称、手机号、跨公众号身份、账号合并或标签画像，再增加用户表和统一内部 userId。

### P11 前端显示已登录，但订单接口返回 401/1006

**问题现象**

浏览器本地还保存 Token，页面因此显示已登录；但 Token 已过期、签名失效或后端重启后状态变化，订单接口返回 HTTP 401 或业务码 1006。

**根因**

前端只判断 localStorage 中是否存在 Token，没有检查 JWT `exp`，也没有统一处理认证失败。

**解决方案**

- 页面初始化时解析 JWT `exp`，过期会话直接清除。
- HTTP 401 和业务码 1006 统一识别为认证失败。
- 清除 `mallLoginToken/mallUserId`，关闭订单等受保护面板。
- 导航恢复未登录状态，并打开扫码登录入口。
- 普通网络错误保留错误提示，不误判为登录失效。

**面试回答**

> 前端登录态不能只看本地有没有 Token。我同时校验 JWT 过期时间，并在后端返回 401/1006 时统一清理会话、恢复未登录 UI、引导重新扫码，避免页面状态和服务端身份不一致。

### P12 新用户、人群标签和首次参团

**容易误解的地方**

新用户第一次登录并不意味着固定不能参加拼团。是否可参加取决于活动规则：

- 普通活动可以让新用户直接参加。
- 如果活动要求某个人群标签，而新用户还没有标签数据，则需要明确默认规则。
- 可以设计“默认人群”“新客标签”或规则未命中时的兜底策略。

人群标签应该绑定稳定 openid/内部 userId，因此不会因为 ticket 或 JWT 变化失效。当前项目已有 `crowd_tags`、`crowd_tags_detail`、`crowd_tags_job` 规则数据，但新用户冷启动规则仍属于需要结合运营策略完善的内容，不能笼统说“新用户一定不能参团”。

## 6. 商品、营销配置和数据库问题

### P13 商品表与拼团四张配置表职责混乱

**问题现象**

只在商城 `product` 表写入商品后，拼团页面可能仍没有活动价格；反过来只配活动也无法获得完整商品详情。

**根因**

商品事实和营销配置属于不同服务：

```text
商城 product
   └─ product_id
        = 拼团 sku.goods_id
        → sc_sku_activity.goods_id
        → group_buy_activity.activity_id
        → group_buy_discount.discount_id
```

**解决方案**

- 预先配置：`product`、`sku`、`group_buy_discount`、`group_buy_activity`、`sc_sku_activity`。
- 运行时写入：`pay_order`、`group_buy_order`、`group_buy_order_list`、`notify_task`。
- 商品基础信息由商城维护；拼团服务只保存 SKU 与活动、折扣、渠道的关联和营销快照。
- 初始化 SQL 使用明确业务 ID 和幂等写法，但执行前必须确认唯一索引存在。

**面试回答**

> 我把商品域和营销域分开：商城维护商品事实，拼团服务维护 SKU、活动、折扣和渠道关系。配置表上线前预置，订单和通知表由真实交易流程写入，避免把测试配置和交易数据混在一起。

### P14 不能从实体类猜生产数据库结构

**问题现象**

Mapper 和 PO 只能看出代码使用了哪些字段，无法证明生产库的字段长度、默认值、非空、主键、唯一索引和外键。

**解决方案**

- `ekko-group-chat/src/main/resources/db/schema.sql` 属于当前完整 DDL 证据。
- 其他业务表的历史 SQL 或归档脚本只作为参考。
- 生产结论必须执行 `SHOW CREATE TABLE`、`SHOW INDEX` 或查询 `information_schema`。
- 新增列/索引前先检查是否已经存在，避免重复执行。
- 应用配置为手工迁移时，修改 `schema.sql` 不等于线上数据库已经升级。

**面试回答**

> 我区分了代码映射、仓库 DDL 和线上真实结构。实体类只能证明字段被代码使用，涉及唯一约束和幂等设计时必须以 `SHOW CREATE TABLE/INDEX` 为准，避免把推断当事实。

## 7. 智能客服 RAG 问题

### P15 文档同步解析阻塞请求，失败后难恢复

**问题现象**

如果上传接口同步等待 PDF 解析、分片、Embedding 和 ES 写入，请求耗时长，第三方解析超时会直接中断，也无法在应用重启后恢复进度。

**解决方案**

```text
上传文件
  → MySQL 创建 UPLOADING 记录
  → 私有 OSS 保存原文件
  → 生成单对象短期签名 URL
  → MinerU 创建异步任务
  → 保存 taskId，状态变为 PARSING，HTTP 201 立即返回
  → 后台轮询 pending/running/converting/done/failed
  → done 后下载 ZIP，安全提取 full.md
  → OSS 保存原始 Markdown
  → INDEXING → 分片/Embedding/ES → PUBLISHED
```

任务超时、MinerU 明确失败、ZIP 不合法、缺少 Markdown、OSS/ES 写入失败统一进入 `FAILED` 并保存原因；临时查询网络失败保留 `PARSING`，下一轮继续查询。

**安全点**

- OSS Bucket 保持私有，只给 MinerU 单对象、短期签名 URL。
- 下载结果 ZIP 时不携带 MinerU API Token。
- ZIP 提取要防路径穿越和异常压缩包。

### P16 分片出现 OCR 异体字、HTML 表格和孤立标题

**真实现象**

- `⽀持`、`⼀代`、`端⼝`、`客⼾端` 等兼容字符影响向量语义。
- MinerU HTML 表格被字符窗口切断，分片可能从 `</td>`、`</tr>` 开始。
- 表格被直接去标签后，参数名与参数值的关系丢失。
- 出现只有 `## 技术参数` 的无意义分片。
- 很短的产品前言和后续“硬件规格”被切成孤立块。

**解决方案**

1. Unicode NFKC 规范化，并使用受控 OCR 修复字典。
2. 使用 Jsoup 解析 HTML 表格，支持 `rowspan/colspan`，转换为完整键值行。
3. 对历史残缺表格边界进行有限容错修复。
4. 标题只用于构建 `headingPath`，不生成标题-only 正文。
5. 小于阈值且合并后不超限的块并入相邻章节。
6. 入库前执行质量校验：空分片、重复 ID、序号不连续、标题-only、残留表格标签、有效字符过少均拒绝发布。

**证据位置**

- `KnowledgeMarkdownPreprocessor`
- `MarkdownTitleChunkingStrategy`
- `ChunkQualityValidator`

**验证结果**

- 客服模块 26 个 Maven 测试全部通过。
- 回放 5 个历史真实分片后不再残留表格标签或闭合标签开头。
- 原 document 12 的 3 个旧块重新处理后成为 1 个有效可检索块，不再保留孤立标题。

**面试回答**

> 我没有直接调 chunkSize，而是先定位到抽取文本质量和结构丢失。入库前做 Unicode/OCR 规范化，把 HTML 表格转成保留行列关系的键值文本，再按 Markdown 结构切分并增加质量门禁，从源头避免垃圾分片进入 ES。

### P17 按字符切分破坏表格行和标签

**问题现象**

旧长度策略在固定字符位置回退，overlap 也从字符中间开始，导致完整段落、列表项和表格标签被截断。

**解决方案**

- 先把 Markdown 拆成段落和完整行原子块。
- 以原子块装箱到目标 `chunkSize`，不从表格行中间拆开。
- overlap 只复用上一分片的完整原子块，实际重叠可能小于配置目标。
- 普通超长段落才退化到句号、换行、空格等自然边界。
- 极端长的单个表格行允许略超字符上限，优先保留单元格关系。

**边界说明**

当前 `chunkSize` 仍以字符为单位，不等于模型 Token 数。如果模型要求严格 Token 上限，还应接入对应 tokenizer 验证。

### P18 分片缺少产品身份，容易跨型号误召回

**问题现象**

很多产品文档都有“支持 MLO”“4 个端口”等相似表达。如果向量文本只有正文，用户询问 TL-7DR6560 时可能召回另一个路由器的相似参数。

**解决方案**

- 展示正文 `displayText` 保持干净，用于回答和引用。
- Embedding 文本额外加入：产品型号、文档标题、章节路径。
- ES metadata 保存规范化大写的 `productModel`。
- 查询中识别 `TL/ARCHER/DECO-*` 型号后，动态生成 ES `productModel` 精确过滤条件。
- 没有明确型号的问题仍走全库语义检索。

**面试回答**

> 我把展示文本和 Embedding 文本分开。向量化时加入型号、文档标题和章节，检索时如果问题出现明确型号，再叠加 metadata filter，这样既减少跨产品误召回，也不会污染最终引用文本。

### P19 小块召回准确，但回答上下文不足

**问题现象**

小分片便于向量精确召回，但模型可能看不到完整步骤和前后约束；大分片上下文完整，但向量主题过多、召回精度下降。

**解决方案**

- 长章节先生成父分片，父分片保存在 MySQL，不参与 Embedding。
- 父分片继续切成较小子分片，子分片写 ES 并参与召回。
- ES 命中子分片后，根据 `parentChunkId` 从 MySQL 回填父分片。
- 同一父分片的多个子命中进行去重，避免重复塞入模型上下文。

**面试回答**

> 我采用“小块检索、大块回答”。子块负责提高召回精度，命中后回填父块补充完整上下文，兼顾检索准确率和回答完整性。

### P20 预处理规则升级后旧索引无法追踪

**问题现象**

修改 OCR 字典、表格转换或分片规则后，ES 中旧数据无法判断使用了哪一版逻辑，也没有便捷方式重建。

**解决方案**

- `knowledge_document` 增加 `preprocess_version` 和 `chunk_version`。
- ES metadata 同时保存两个版本。
- 新增 `POST /api/v1/documents/{id}/reindex`。
- 重新索引直接读取 OSS 中保留的原始 MinerU Markdown，不再次上传文件或调用 MinerU。
- 首次解析和重新索引共用 `KnowledgeIndexingService`，避免两套规则漂移。

**部署注意**

已有数据库必须先执行：

```text
ekko-group-chat/src/main/resources/db/migration/
2026-08-14-add-knowledge-index-versions.sql
```

当前源码和测试已完成，但真实 MySQL 迁移、OSS 读取和 ES 重建仍需在部署环境验证。

### P26 MinerU解析后图片路径失效，图片内容无法参与检索

**发现日期**：2026-08-14  
**所属模块**：客服 / RAG / OSS / 数据库  
**当前状态**：已实现并通过单元测试；真实 MinerU、OSS 和视觉模型尚未联调

**问题现象**

MinerU 的 `full.md` 中存在 `![](images/xxx.jpg)`，但系统下载结果 ZIP 后只提取并保存 `full.md`，没有保存 `images/`。ES 中因此出现无法访问的相对路径，Embedding 模型只能看到图片文件哈希，无法理解图片内容。

**影响范围**

产品外观、接口布局、安装示意图和图片参数无法检索；聊天来源也无法向用户展示原图。

**根因**

旧 `MineruArchiveParser` 找到 `full.md` 后立即返回，ZIP 中其他条目全部忽略；Markdown、MySQL、ES 和问答 DTO 也没有图片身份与生命周期设计。

**解决方案**

- ZIP 解析结果改为同时返回原始 Markdown 和经过魔数校验的图片，保留 Zip Slip、文件大小、图片数量和累计解压大小限制。
- 图片按文档 SHA 和图片 SHA 使用确定性路径写入私有 OSS，MySQL `knowledge_image` 保存 object key、描述状态、模型和版本。
- 视觉模型通过短期 OSS 签名 URL 读取图片并生成可检索描述；单张描述失败只记录状态，不阻塞整篇文档发布。
- 原始 Markdown 与处理后 Markdown 分开保存；相对路径改为稳定的 `knowledge-image://{id}`，Embedding 前去除 URL 和哈希，只保留图片描述。
- `knowledge_chunk_image` 保存分片和图片关系，问答命中分片后临时生成 OSS 签名 URL 返回图片。
- 商城客服来源卡片渲染命中分片的图片缩略图，点击后查看临时签名原图。

**验证结果**

Java 21 下使用 Mockito Java Agent 执行模块测试，ZIP 图片提取、Zip Slip 拦截、图片路径改写、Embedding 清洗和问答图片来源测试通过。尚未执行数据库迁移，也未验证真实 MinerU ZIP 是否包含全部图片、OSS 上传和视觉模型图片理解效果。

**遗留风险**

旧 ZIP 未持久化，历史文档必须重新调用 MinerU 才能补图片；当前是“视觉描述转文本后检索”，还不是图片原生向量检索；多图片文档的视觉模型耗时和费用需要监控。

**面试回答**

> MinerU 返回的是包含 Markdown 和图片的结果 ZIP，旧实现只取 `full.md`，导致相对图片路径失效。我把解析结果改成 Markdown 加图片资源，图片用 SHA 命名存入私有 OSS，再由视觉模型生成描述参与文本向量检索。数据库只保存稳定 object key 和 imageId，回答时才生成短期签名 URL，并通过分片图片关联只返回命中内容相关的图片。

### P27 长文档向量化超过模型单批数量限制

**发现日期**：2026-08-14  
**所属模块**：客服 / RAG / Embedding / Elasticsearch  
**当前状态**：已解决并通过单元测试；真实模型接口重试待验证

**问题现象**

长文档产生较多可检索分片后，Embedding 接口返回 `InvalidParameter`。初步错误曾提示单批不能大于 20，但当前 `text-embedding-v4` 实际调用栈明确返回单批不能大于 10，说明限制取决于具体模型服务或路由，不能把其他接口的上限直接套用过来。

**影响范围**

普通分片和子分片总数超过模型限制的文档无法完成向量化，因而不能发布到知识库；父分片不参与 Embedding，不计入该批次数量。

**根因**

旧实现直接对整篇文档的 `embeddingSegments` 调用一次 `embedAll`，只考虑了减少逐条网络请求，没有适配模型供应商的单批输入数量上限。

**解决方案**

- 增加 `group-chat.rag.embedding-batch-size`，根据当前服务实测结果默认并限制为 10。
- 按原始分片顺序将待向量化文本切成 `10 + 10 + ... + 余数` 的批次，串行调用 `embedAll`。
- 校验每批返回向量数量必须等于请求数量，避免向量和 `chunkId` 错位。
- 所有批次完成后再按 `documentId` 删除 ES 旧索引并整体写入新向量；任何中间批次失败时保留旧索引。

**验证结果**

单元测试构造 21 个分片，验证模型收到三次请求且批次大小依次为 10、10 和 1；另一个测试模拟第二批失败，验证 ES 存储没有执行删除或写入。真实日志已证明错误来自 LangChain4j `OpenAiEmbeddingModel.embedAll()`，但修改后的批次还需要在当前模型服务上重新索引验证。

**遗留风险**

当前只解决了单批条数限制；模型仍可能存在单批 Token 总量、请求频率和并发限流。真实环境需要结合 429/5xx 响应增加带退避的有限重试和监控，不能无限重试。

**面试回答**

> 长文档分片较多时，模型接口因单批输入数量超限返回 400。根因是旧代码把整篇文档一次传给 `embedAll`，并且不同模型服务的上限并不一致。根据当前接口实测，我将向量化改为最多 10 条一批串行处理，并校验每批响应数量；全部批次成功后才替换 ES 旧索引，因此既满足接口限制，也避免中途失败先删除线上可用数据。

### P28 Embedding 从 1024 维升级到 1536 维后空索引仍冲突

**发现日期**：2026-08-14  
**所属模块**：客服 / RAG / Embedding / Elasticsearch  
**当前状态**：已实现并通过单元测试；真实 ES 建索引和模型重新向量化待验证

**问题现象**

计划把 `text-embedding-v4` 输出从 1024 维提高到 1536 维。即使业务分片和 ES 中的向量文档已经全部清空，继续使用旧索引名仍可能在写入时报 `vector` 维度不匹配。

**影响范围**

维度错误会让整篇文档无法写入 ES；如果只修改模型参数而没有同步切换索引，上传和重新索引都会失败。不同维度的向量也不能直接相互计算相似度。

**根因**

ES 的文档数据和索引 mapping 生命周期不同。删除文档只会清空数据，不会删除 `dense_vector` 字段的 `dims=1024` 定义；该字段维度不能原地修改为 1536。

**解决方案**

- 将模型配置改为 `text-embedding-v4 + 1536`，并新增 `embeddingVersion=text-embedding-v4-1536-v1`。
- 使用全新版本化索引 `group_chat_knowledge_chunk_v2_1536`，与旧 1024 维索引物理隔离。
- 首次入库时显式创建 `vector: dense_vector, dims: 1536, similarity: cosine` 映射；已有索引则先读取 mapping 校验维度。
- 每批模型响应除校验向量数量外，再逐条校验实际维度；出现 1024/1536 不一致时，在删除旧数据前终止。
- ES 元数据增加 `embeddingModel`、`embeddingDimension`、`embeddingVersion`，便于定位混用和后续版本迁移。
- MySQL 表结构不需要修改；现有文档需要通过重新索引或重新上传，让全部分片重新生成 1536 维向量。

**验证结果**

单元测试覆盖 1536 维正常写入元数据、模型误返 1024 维时拒绝替换数据、ES mapping 为旧 1024 维或不是 `dense_vector` 时拒绝写入。Java 模块编译和相关测试通过。验证时本机 ES 未运行，因此尚不能把“新索引已在真实 ES 创建、全部文档已重新向量化、检索质量已提升”作为已完成证据。

**遗留风险**

1536 维会增加向量存储、网络传输和近邻检索开销；是否优于 1024 维必须用真实问题集比较 Recall@K、引用准确率和延迟。后续再次换模型或维度仍应新建索引并全量重建，不应直接覆盖现有索引。

**面试回答**

> 我把 Embedding 从 1024 维升级到 1536 维时，注意到清空 ES 文档并不等于清空 mapping，旧索引的 dense_vector 仍固定为 1024 维。解决方式是切换到带版本的新索引，在首次写入前显式创建和校验 1536 维映射，同时校验模型实际返回的每条向量长度，并在元数据记录模型、维度和版本。这样配置错误会在替换数据前失败，也为后续全量重建和回滚保留了清晰边界。

### P29 上传文件不能只相信扩展名或客户端 MIME

**发现日期**：2026-08-30  
**所属模块**：客服 / RAG / 文档上传 / 文件类型检测  
**当前状态**：已实现并通过单元测试与 Maven 干净打包；真实 OSS/MinerU 上传待验证

**问题现象**

旧上传流程按文件扩展名选择本地转换器或 MinerU，并把浏览器提交的 `Content-Type` 直接写给 OSS。纯文本改名为 `.pdf`、PDF 改名为 `.txt` 等情况可能进入错误处理链路，失败位置晚且错误信息不准确。

**根因**

文件名和 multipart `Content-Type` 都由客户端提供，不代表文件真实内容。参考 KnowEngine 的 Tika 方案后进一步核对发现，仅采用“扩展名命中或内容命中”仍会放过扩展名与内容冲突的文件。

**解决方案**

- 引入与 KnowEngine 一致的 `org.apache.tika:tika-core:2.9.1`。
- 上传读取字节后先由 `TikaFileTypeDetector` 检测内容，再校验检测结果与扩展名兼容；校验通过后才查询数据库、上传 OSS 或提交 MinerU。
- OSS 使用服务端规范化后的 MIME，不信任客户端声明。
- 对 Tika Core 只能识别为 ZIP 的 DOCX/XLSX，额外检查 `[Content_Types].xml` 与 `word/document.xml` / `xl/workbook.xml` 核心条目，避免普通 ZIP 冒充 Office 文档。

**验证结果**

单元测试覆盖 PDF 真内容、伪装 PDF、PDF 伪装 TXT、Markdown/CSV、DOCX/XLSX 与普通 ZIP 区分、未知扩展名，以及上传主流程在落库和调用外部服务前拒绝伪装文件。`mvn -o clean package` 完成 100 个主源码、21 个测试源码的全量编译，并通过 61 个测试。

**遗留风险**

旧版 OLE 容器中的 `.doc` 与 `.xls` 仅靠 Tika Core 可能返回通用 Office MIME，无法像 OOXML 一样通过 ZIP 条目精确区分；真实 OSS、MinerU 和不同来源 Office 样本仍需做集成验证。文件检测也不是杀毒扫描，生产上传仍应保留大小限制、超时和恶意文件隔离策略。

**面试回答**

> 文档上传原先按扩展名和客户端 MIME 路由，伪装文件会到解析阶段才失败。我参考 KnowEngine 引入 Tika，但没有照搬它的“扩展名或内容命中”判断，而是要求扩展名与真实内容兼容，并让 OSS 使用服务端规范 MIME；DOCX/XLSX 再检查 OOXML 核心条目。这样错误文件会在落库和外部调用前失败，单测也验证了伪装文件不会触发 OSS 或 MinerU。

### P30 悬浮客服怎样扩展为多会话工作台

**发现日期**：2026-08-31  
**所属模块**：商城前端 / 智能客服 / 会话交互  
**当前状态**：已实现并通过前端测试、生产构建和桌面/移动浏览器验证

**问题现象**

商城原来只有右下角悬浮客服，一条消息流适合临时问一个商品问题，但用户连续咨询参数、组网和故障时，所有内容会混在同一个窗口里，也无法按主题回看。

**根因**

原 `ChatAssistant` 组件只维护一个内存中的 `messages` 数组，页面刷新后丢失，也没有会话身份、会话列表和移动端会话导航。后端 `POST /api/v1/chat` 当前只接收 `question`，不存在服务端会话存储或多轮 ChatMemory。

**解决方案**

- 保留首页右下角悬浮客服，继续满足选购过程中随手提问的短路径。
- 首页顶部增加“智能客服”入口，进入 `#/assistant` 独立工作台；参考 KnowEngine 的信息架构，实现左侧会话列表和右侧消息区，但沿用联巢商城的颜色、字体和响应式体系。
- 浏览器端为每个会话生成独立 ID，支持新建、切换和删除；首个问题自动生成简短标题，最近 24 个会话写入 `localStorage`。
- 异步问答始终记录发起时的会话 ID，即使等待期间切换页面，回答也只写回原会话，避免串话。
- OSS 图片是短期签名 URL，只在当前响应展示，不写入长期会话存储；历史记录保留来源标题等稳定信息。
- 手机端把会话列表改为可关闭抽屉，聊天区和输入框保持在 `100dvh` 内，避免页面横向溢出。

**验证结果**

新增会话状态单测，覆盖默认欢迎语、首问标题、损坏存储回退和短期图片 URL 不持久化。前端共 14 个测试全部通过，`vite build` 成功。浏览器在 1440×900 和 390×844 下验证无横向滚动；新建会话后列表为两条且只有一个激活项，移动端抽屉能打开和关闭，浏览器控制台无错误。后端服务未在该次页面验证中启动，因此没有把真实 RAG 答案响应列为本次联调证据。

**遗留风险**

当前多会话是单浏览器本地整理能力，不会跨设备同步，也没有把历史消息传给后端，因此不是服务端多轮记忆。若以后需要账号级历史和上下文追问，应新增会话/消息表、按 JWT userId 鉴权的列表与删除接口，并只截取受控长度的历史传入 RAG，不能直接把全部聊天记录塞给模型。

**面试回答**

> 商城原先只有悬浮客服，适合临时提问，但不同主题会混在一条消息流里。我保留这个低打扰入口，同时参考 KnowEngine 的会话信息架构新增独立客服工作台，用会话 ID 隔离异步响应并在浏览器保存最近记录；移动端则把会话列表做成抽屉。实现时我明确区分了“前端多会话分组”和“后端多轮记忆”，当前后端仍是单问单答，避免把 UI 能力夸大成模型上下文能力。

### P31 商城和客服页的信息层级怎样适配不同任务

**发现日期**：2026-08-31  
**所属模块**：商城前端 / 智能客服 / 响应式 UI  
**当前状态**：已实现并通过前端测试、生产构建和桌面/移动浏览器验证

**问题现象**

商城首页原来更像品牌展示页：主视觉和叙事卡片占据大量空间，但拼购价格、购买方式和操作按钮不够突出；独立客服页则使用大圆角外框、纹理背景和气泡卡片，视觉上更像运营后台，不利于长内容阅读。两类页面沿用同一套视觉语言，没有体现“商城快速选购”和“客服专注阅读”这两个不同任务。

**根因**

- 商城卡片强调留白和品牌氛围，交易状态、价格与动作没有形成明确优先级。
- 客服页同时存在页面外框、工作区卡片、消息气泡三层容器，装饰层级多于内容层级。
- 响应式样式采用文件末尾覆盖时，后写的桌面 `grid-template-columns` 可能反向覆盖先前的移动断点。实际浏览器验证中发现过客服手机端仍保留 `260px + 剩余区域` 的桌面网格，导致输入框只有约 236px 宽。

**解决方案**

- 商城参考主流拼购电商的信息组织方式，但不复制品牌素材：增加促销说明条、白色商城导航、红色拼购主视觉、选购分类条和“热门设备”区；使用红色强调多人拼价和“去拼购”按钮。
- 商品卡在桌面保持高密度多列，手机端保持双列；描述限制为两行，并让型号、购买方式、价格和动作始终可见。不展示虚构销量、倒计时或补贴金额，实际金额仍明确以服务端为准。
- 拼购攻略从三张超高叙事卡改为桌面三列短卡；保障条、场景参考和行动区统一为交易型红色视觉。
- 客服页参考 OpenAI 对话工作区的信息架构：使用中性灰侧栏、无外层卡片的全高工作区、最大 760px 的居中消息流、弱化后的用户气泡，以及固定在底部的圆角输入框。
- 保留多会话、新建/切换/删除、来源图片、异步会话隔离和移动端抽屉功能，只调整视觉与布局，不改变后端 RAG 协议。
- 在最后一层移动断点中再次显式设置客服工作区 `grid-template-columns: 1fr`，避免桌面覆盖规则使手机主区变窄；顶部促销条固定 32px 并让品牌文字不换行，避免与导航重叠。

**验证结果**

`npm test -- --run` 共 14 项通过，`npm run build` 成功。浏览器在 1440×900 下验证商城无横向溢出，客服侧栏为 260px、输入框为 760px；在 390×844 下验证商城商品网格为双列、促销条与导航不重叠，客服主区为 390px、输入框为 366px，且会话抽屉能正常覆盖打开。两个页面的浏览器控制台均无前端错误。

该次视觉验收时商城服务 8092 未启动，因此页面展示的是商品服务不可用的占位状态；真实商品数据、下单抽屉和支付流程没有被本次视觉验证重新声明为端到端联调完成。

**遗留风险**

拼购风格是否能提升商品点击率和下单转化，需要真实埋点或可用性测试验证；当前只完成了任务层级与响应式质量改造。客服页仍是浏览器本地多会话，后端不保存多轮上下文，这一边界与 P30 相同。

**面试回答**

> 我没有把商城和客服强行做成同一种页面。商城的任务是快速比较和下单，所以我重排为促销说明、分类、商品、价格和操作的高密度层级；客服的任务是长时间阅读和追问，所以改为中性侧栏、居中消息流和底部输入框。响应式验收还发现桌面网格覆盖了移动断点，我通过实际测量把客服手机主区从约 260px 修回全宽，并在桌面、手机尺寸上验证无横向溢出。

### P32 悬浮客服与改版后的商城风格不统一

**发现日期**：2026-08-31  
**所属模块**：商城前端 / 智能客服 / 响应式 UI  
**当前状态**：已解决并通过前端测试、生产构建和桌面/移动浏览器验证

**问题现象**

商城已经切换为红色拼购视觉，但首页右下角悬浮客服仍使用荧光绿头像、米色网格背景、三层大圆角卡片和 680×820 的大面板。用户打开客服后会从交易型商城突然进入另一套视觉体系，而且面板遮挡了过多商品内容。

**根因**

悬浮 `ChatAssistant` 与独立 `CustomerServicePage` 是两个不同组件。P31 只重构了商城和独立客服页，没有同步覆盖旧悬浮客服中以 `--lime`、`--cream` 为核心的样式，因此同一页面同时出现红色交易强调和绿色客服强调。

**解决方案**

- 保留 `ChatAssistant` 的提问、快捷问题、来源图片、加载态和关闭逻辑，只修改文案和样式。
- 桌面面板由 680×820 收敛为最大 520×640，减少对商品区的遮挡。
- 删除米色网格、荧光绿外环和绿色消息气泡；助手标识、顶部细线、焦点态和可用发送按钮统一使用商城红。
- 助手回答改为白底无气泡阅读流，用户消息使用浅灰气泡，快捷问题改为单列中性按钮，与独立客服页保持一致。
- 手机端使用安全边距和 `100dvh` 限制。浏览器验证时发现 `100vw` 包含滚动条后面板左侧被裁掉约 5px，最终将宽度收敛为 `calc(100vw - 32px)`。

**验证结果**

`npm test -- --run` 14 项通过，`npm run build` 成功。桌面 1440×900 下实测面板为 520×640、输入框 440px；手机 390×844 下实测面板宽 358px、左侧保留 7px 可见安全边距，页面无横向滚动，浏览器控制台无前端错误。

验证时商城服务 8092 未运行，商品接口代理返回连接失败，因此本次证据只覆盖前端样式、交互打开和响应式布局，不包含商品或客服后端端到端联调。

**遗留风险**

悬浮客服和独立客服仍各自维护消息状态，从悬浮框进入独立页面时不会自动带入当前对话。若以后要统一会话，应先确定产品交互，再抽取共享会话 store，不能仅靠 CSS 合并。

**面试回答**

> 商城切换到红色拼购风格后，我发现旧悬浮客服仍是荧光绿和米色网格，打开后像进入另一个产品。我没有重写问答逻辑，而是把面板收敛到 520×640，统一商城红和中性消息流；随后用桌面和手机浏览器实测。移动端还发现 `100vw` 包含滚动条导致左边缘被裁掉，我通过增加安全边距修正并验证无横向溢出。

### P33 商城文案为什么不能直接使用后端和系统术语

**发现日期**：2026-08-31  
**所属模块**：商城前端 / 下单与订单 / 智能客服  
**当前状态**：已解决并通过前端测试、生产构建和桌面/移动浏览器验证

**问题现象**

商城首页直接向消费者展示“金额由服务端确认”“支付结果同步”“状态持续更新”“路由器选购与拼团体验”等系统实现或项目说明文案。空状态和错误提示中也出现“商品服务”“重新请求”等开发视角表达。虽然语义正确，但用户更关心到手价、怎么买、去哪里看进度以及遇到问题怎么办。

**根因**

- 页面文案沿用了接口、状态机和项目文档中的技术表述，没有区分面向开发者的事实描述与面向消费者的行动提示。
- 改造时更关注功能和视觉一致性，缺少一次覆盖首页、下单、订单、登录和客服的完整内容审查。
- 部分场景卡为了说明系统能力，写成了“服务端确认、订单状态同步”一类架构说明，弱化了消费者能获得的实际价值。

**解决方案**

- 按“浏览商品 → 选择购买方式 → 确认价格 → 支付 → 查看订单 → 售后”的消费者旅程统一文案，不再按后端模块命名页面能力。
- 将“金额由服务端确认”改为“下单前看清价格”，将“实际价格和可用活动以服务端返回为准”改为“优惠和到手价以下单页面显示为准”。这里仍保留了价格以订单确认为准的业务边界，但不暴露系统实现。
- 将“支付结果同步到订单中心”“订单与拼团状态持续更新”分别改为“下单后到‘我的订单’看进度”和“订单和拼购进度一目了然”。
- 将“履约、商品服务、请求失败、演示模式”等词分别改成“安排发货、商品暂时没有加载出来、重新加载、体验模式”等用户熟悉的表达。
- 将智能客服从“知识库支持”的技术定位改为“路由器选购助手”，错误提示说明用户下一步可以稍后重试或换个问法。
- 将原来的姓名式评价内容改成“全屋覆盖、多设备上网、好友拼购”三个真实选购场景，避免没有事实来源的虚构用户评价。
- 后端类名、接口状态、数据一致性说明和技术边界继续保留在管理端、代码和项目文档中，不因消费者文案简化而丢失。

**验证结果**

`npm test -- --run` 共 14 项通过，`npm run build` 成功。浏览器在 1440×900 和 390×844 下分别检查促销条、价格说明、购买攻略、保障条、客服与页脚；页面无横向溢出，攻略卡片文字没有溢出，手机商品区保持双列，浏览器控制台无前端错误。

本次只调整前端展示文案和少量语义标签，没有修改接口、数据库、价格计算或订单状态机。验证时后端服务未参与，因此不把本次结果描述为完整交易链路联调。

**遗留风险**

文案是否真正提升消费者理解和转化，仍需通过真实用户访谈、埋点漏斗或 A/B 测试验证。以后新增页面时，应在评审清单中加入“是否暴露内部术语”和“用户下一步是否明确”两项，避免问题再次出现。

**面试回答**

> 我在商城验收时发现，功能虽然正确，但页面写着“服务端返回、状态同步、履约”等开发术语，消费者很难快速知道下一步。我按浏览、下单、支付、查进度和售后的购买旅程统一重写文案，同时保留价格以下单确认为准的业务边界。最后用测试、构建和桌面/手机浏览器检查，确认文案替换没有造成溢出或交互回归。

### P34 首页标题和主图怎样同时适配商城定位与响应式布局

**发现日期**：2026-08-31  
**所属模块**：商城前端 / 首页首屏 / 图片资产  
**当前状态**：已解决并通过前端测试、生产构建和桌面/移动浏览器验证

**问题现象**

首页使用“好路由，一起拼更省”作为两行标题，文案本身需要保留，但原字号和行高过大，容易在宽屏或浏览器缩放后占据过多空间。原米白色家居主图与红色商城背景不统一；第一次换成红色产品图时又把图片铺满首屏右半区域，导致路由器和天线过大，与标题争抢焦点。图片缩小并去掉边框后，原图自身携带的红色摄影棚背景仍会形成明显矩形色块，CSS 渐隐只能软化边缘，不能真正消除背景。

**根因**

- 首轮调整把“优化标题外观”误解成“重写标题文案”，改变了用户已经确认的品牌口号。
- 标题字号主要按大屏视觉冲击设置，没有限制合适的最大宽度、行高和字距。
- 原主图的暖米白背景、绿色玻璃和红色商城配色不属于同一视觉系统；外层边框和阴影进一步强化了卡片拼贴感。
- 新图第一次采用右侧整高全幅布局，桌面宽度最多达到 900px，图片主体被进一步放大；后续虽然缩小尺寸并增加径向蒙版，但图片内部背景颜色与页面背景仍有差异，矩形范围仍然可辨认。
- 图片生成结果不能凭预览判断透明度：连续两次输出看似棋盘格透明，使用 `sips -g hasAlpha` 检测却均为 `no`，说明棋盘格已经写入 RGB 像素；如果直接接入会把假透明背景显示给用户。

**解决方案**

- 完整恢复原来的 kicker、标题、说明和按钮文案：“路由器拼购专区 · 按需挑选”“好路由，一起拼更省”“立即逛拼购”“查看拼购商品”。
- 只优化排版：标题最大宽度收敛为 590px，字号调整为 `clamp(3.2rem, 4.25vw, 4.55rem)`，并细调字距和行高，保持两行结构但不再铺满左侧区域。
- 保留无品牌白色 Wi-Fi 7 路由器与两个 Mesh 节点的设备组合，先生成纯色绿幕版本，再使用图片处理技能自带的 `remove_chroma_key.py` 提取背景；脚本从边缘采样到 `#13f010`，输出 1536×1024 RGBA PNG，其中 1,178,868 个像素完全透明、61,298 个像素部分透明。
- 使用 `sips -g hasAlpha` 和 `file` 双重校验最终 `router-hero-cutout-v3.png`：结果分别为 `hasAlpha: yes` 和 `RGBA`，避免把预览中的假棋盘格当成透明图。
- 直接引用透明产品切图，彻底删除径向蒙版、同色柔光和 `mix-blend-mode: lighten` 等补救样式，只保留轻量 `drop-shadow` 作为自然落地阴影。
- 桌面图片画布限制为 `min(39vw, 560px)`，由于透明留白，实际设备视觉宽度约 460px；560px 以下画布限制为 `min(90vw, 340px)`，实际设备视觉宽度约 280px，既保持完整天线又不与按钮相撞。

**验证结果**

`npm test -- --run` 共 14 项通过，`npm run build` 成功。透明主图源文件约 497 KB，保留真实 Alpha 通道和 1536×1024 分辨率。

浏览器在 1440×900 下验证首屏高 516px，透明图片画布为 560×373px，文案区与图片区没有重叠；在 390×844 下验证标题、说明与按钮区域在约 383px 结束，340×227px 的图片从约 405px 开始，操作区与图片没有重叠。页面断言同时确认实际资源已经切换为 `router-hero-cutout-v3.png`、`mask-image` 为 `none`、混合模式为 `normal`。两个尺寸下页面 `scrollWidth` 与 `clientWidth` 相等，没有横向滚动，图片加载完成，浏览器控制台没有前端错误。

验证时商城后端服务未参与，因此本次证据只覆盖标题、图片资源、响应式布局与前端构建，不扩展为商品或交易链路联调完成。

**遗留风险**

当前比例基于 1440px 桌面和 390px 手机进行验证，超宽屏和高倍浏览器缩放仍应纳入后续视觉回归。以后收到“优化外观”类需求时，必须先区分“保留文案只改排版”和“允许重写内容”，避免扩大修改范围。生成式商品主图仍需检查品牌侵权、产品真实性、文件体积和实际 Alpha 通道。

**面试回答**

> 首页原来的问题是标题和图片比例失衡，不是文案内容本身。我先恢复全部原文案并收敛标题比例，又发现图片即使缩小、去掉边框并增加渐隐，原图自带的红色背景仍会形成矩形色块。最终我改成真实透明产品切图：先生成纯色绿幕，再用受控色键脚本提取 Alpha，并用 `sips` 和文件格式双重校验，避免把预览棋盘格误当透明。接入后删除 CSS 蒙版和混合模式，只保留自然阴影，最后通过 14 项测试、生产构建和桌面/手机浏览器断言确认无重叠、横向滚动或控制台错误。

### P35 为什么删除首页顶部促销栏而不是继续压缩高度

**发现日期**：2026-08-31  
**所属模块**：商城前端 / 首页导航  
**当前状态**：已解决并通过前端测试、生产构建和桌面/移动浏览器验证

**问题现象**

首页最上方固定显示“LinkNest 拼购商城”和促销口号，下一行主导航又重复展示品牌与“拼购商城”。两层固定区域不仅信息重复，还占用 34px 首屏高度，使用户进入页面后先看到两层导航结构，削弱商品首屏的聚焦感。

**根因与解决方案**

促销栏最初用于快速建立拼购氛围，但主导航和首屏文案完善后，其品牌与利益点已经被重复表达。继续缩小字号或高度只能减轻占位，不能解决信息重复。因此直接删除 `mall-promo-strip` 模板和桌面/手机响应式样式，并把主导航的固定位置从 `top: 34px/32px` 统一调整为 `top: 0`。其他导航内容、首屏文案和交互保持不变。

**验证结果**

浏览器删除前断言显示促销栏高度为 34px、主导航顶部为 34px，预期失败；删除后桌面和 390px 手机宽度下促销栏均不存在、主导航顶部均为 0，页面没有横向溢出，浏览器控制台没有错误。`npm test -- --run` 共 14 项通过，`npm run build` 成功。

**面试回答**

> 首页曾同时存在促销栏和主导航，两层都展示商城品牌，属于信息重复而不是单纯高度过大。我没有继续压缩促销栏，而是删除重复层级，把主导航统一贴顶，并通过删除前后的浏览器断言验证实际布局变化，再运行完整前端测试与生产构建确认没有回归。

## 8. 动态配置和工程排障问题

### P21 动态配置修改后怎样让多个实例生效

**问题现象**

限流、降级等配置如果只存在 application.yml，修改后必须重启；如果只改某个 JVM 字段，其他实例不会同步。

**解决方案**

```text
DCCController.update_config
  → Redis Bucket 保存最终值
  → Redis Topic 发布 AttributeVO
  → 每个实例订阅消息
  → BeanPostProcessor 维护本地 dccBeanGroup
  → 反射更新 @DCCValue 字段
```

Redis Bucket 是共享配置事实，Redis Pub/Sub 用于广播；`dccBeanGroup` 只是每个 JVM 的本地 Bean 索引。

**遗留风险**

DCC 更新接口和拼团 lock/settlement/refund 接口仍应增加明确的服务间认证，不能只依赖内网地址。

### P22 IDEA 没有运行任务，但端口仍被占用

**问题现象**

启动时出现 `Web server failed to start. Port 8091/8092 was already in use.`，但 IDEA 界面没有显示正在运行。

**定位方法**

```bash
lsof -nP -iTCP:8091 -sTCP:LISTEN
lsof -nP -iTCP:8092 -sTCP:LISTEN
ps -p <PID> -o pid,ppid,command
```

通过监听端口、进程命令和 cwd 确认是否为旧 Spring Boot 实例。历史排查中发现过由 IDEA 启动、随后父进程退出并变成 `PPID=1` 的 Java 进程，所以 IDEA 界面停止不等于操作系统进程一定退出。

**解决原则**

先确认 PID 和项目路径，再终止指定进程；随后重跑同一个 `lsof` 命令确认端口释放。不要看到端口占用就批量 kill Java。

### P23 Maven 报“无效的目标发行版 21”

**问题现象**

项目要求 Java 21，但 Maven 编译报：

```text
Fatal error compiling: 无效的目标发行版: 21
```

**根因**

真正运行 Maven 的 JVM 不是 Java 21。本机 `.mavenrc` 曾强制配置 Java 8，因此终端 `java -version` 正确也不能证明 Maven JVM 正确。

**解决方案**

```bash
MAVEN_SKIP_RC=true mvn -v
```

确认 Maven 输出的 Java version 和 JAVA_HOME，再执行构建；IntelliJ 还要分别检查 Project SDK、Maven Importer JDK 和 Maven Runner JRE。

**面试回答**

> 这个错误不是源码不支持 Java 21，而是 Maven 进程实际跑在旧 JDK 上。我先用 `mvn -v` 检查构建 JVM，再修正 Maven Runner 或绕过本机 rc 配置。

### P24 Mockito 在 JDK 21 下无法自挂载

**问题现象**

测试出现：

```text
Could not initialize plugin: interface org.mockito.plugins.MockMaker
Could not self-attach to current VM using external process
```

**根因**

Mockito inline mock maker 需要 Byte Buddy Instrumentation；受限环境或较新 JDK 不允许测试进程运行时自附加。

**解决方案**

把 Mockito JAR 作为 Surefire 测试 JVM 的 `-javaagent` 显式加载，而不是把环境错误误判成业务断言失败。

示例：

```bash
MAVEN_SKIP_RC=true mvn \
  -DargLine="-javaagent:<mockito-core-jar绝对路径>" \
  test
```

注意 `MAVEN_OPTS` 只作用于 Maven 主进程时，未必会传到 Surefire fork 出来的测试 JVM，因此应使用 Surefire `argLine`。

### P25 管理员登录页视觉右偏

**问题现象**

旧页面使用双栏布局，即使登录卡片在右侧栏内部居中，视觉上仍然偏右；移动端还可能出现横向溢出。

**解决方案**

- 移除不需要的左侧展示区，改成单卡片 centered flex 布局。
- 保持登录 API、错误提示和跳转逻辑不变。
- 使用浏览器实际测量 `horizontalCenterOffset`，并检查桌面和移动尺寸是否横向溢出。
- 不能只通过阅读 CSS 判断“应该居中”。

### P36 生产密钥硬编码并被打入 JAR

**发现日期**：2026-09-02  
**所属模块**：拼团 / 商城 / 客服 / 部署  
**当前状态**：制品与服务器注入已验证，已暴露旧密钥仍需人工轮换

**问题现象**

商城、拼团和客服配置中存在数据库、RabbitMQ、支付、微信、OSS、MinerU、模型服务等明文凭据。商城与拼团 POM 又会打包整个 `src/main/resources`，导致被 Git 忽略的 `application-dev/test/prod.yml` 仍进入本机构建出的 JAR。

**影响范围**

JAR、构建缓存、制品库或服务器文件一旦被读取，可能同时泄漏数据库和第三方平台权限；不同环境也无法安全复用同一制品。

**根因**

把“不进入 Git”误当成“不会进入制品”，同时缺少统一的生产变量清单和 Maven resources 排除规则。微信工具类中还遗留了一个写死的回调 Token。

**定位过程与证据**

解包旧制品时可看到 `application-dev.yml`、`application-test.yml`、`application-prod.yml`；源码配置中可直接定位到明文值。旧 POM 的 resources 使用 `**/**`，没有排除本地 profile 文件。

**解决方案**

- 三个服务统一从环境变量读取数据库、中间件、JWT、支付、微信、OSS、MinerU 和模型凭据。
- 新增 `deploy/production/market.env.example`、`mall.env.example`、`chat.env.example`，服务器使用独立环境文件。
- market/pay-mall 的 Maven resources 关闭过滤，并排除所有本地 profile 和 secret 配置。
- 清空本机 profile 文件中的旧凭据；`SignUtil` 改为由调用方传入 Token。
- 根 `.gitignore` 忽略真实 `deploy/env/*.env`，但保留可提交的 example 模板。

**验证结果**

- market 14、pay-mall 60、group-chat 60 个单元测试全部通过，三个 JAR 均构建成功。
- 解包后每个 JAR 只包含一个 `application.yml`，不再包含 dev/test/prod profile。
- 制品内配置保持环境变量占位符，长私钥、AccessKey、API Key 特征扫描无命中。
- 三个服务已在生产服务器使用独立环境文件启动，真实 RDS、Redis、RabbitMQ 和 Elasticsearch 连接正常，三个 Actuator 健康检查均为 `UP`。
- 服务器真实环境文件权限为 `600`，对外只开放 22、80 和 443，后端与中间件端口均未映射到公网。
- 支付、OSS、MinerU 和模型服务没有重用已暴露旧密钥；相应功能在获得轮换后的新值前保持关闭或不可用。

**遗留风险**

源码清理不能让已经暴露过的凭据自动失效。SSH、RDS、应用管理员以及支付宝、微信、OSS、MinerU 和模型服务的旧凭据必须在各自控制台轮换，再更新服务器密钥文件。RDS 实例当前未启用 SSL，JDBC 只能临时使用 `sslMode=PREFERRED`；生产安全闭环需先在 RDS 启用 SSL，再改回 `REQUIRED`。

**面试回答**

> 我在发布审计中发现，配置文件即使被 Git 忽略，仍可能因为 Maven 打包整个 resources 而进入 JAR。我将三个服务统一改为运行时环境变量注入，给每个服务提供独立 EnvironmentFile 模板，并在构建层排除本地 profile。最后通过全量单测、JAR 解包和凭据特征扫描验证制品只保留变量占位符；历史凭据则要求在外部平台单独轮换。

### P37 Maven 生产 Profile 不生效且 2C4G 资源参数过大

**发现日期**：2026-09-02  
**所属模块**：拼团 / 商城 / 部署  
**当前状态**：配置侧已解决并验证，容量上限仍需预发布压测

**问题现象**

两个根 POM 虽然定义了 dev/test/prod Maven Profile，但其中 `profileActive` 和 `java_jvm` 没有任何插件或脚本引用。prod 块还写成 `spring.profiles.active=release`，堆内存配置为 6GB；在 2C4G 主机上即使该参数真正生效也无法安全运行。应用配置则默认回退 dev，生产环境漏配时可能连接本地依赖或暴露开发端点。

**影响范围**

构建命令 `mvn -Pprod` 给人“已经选择生产环境”的错觉，实际运行环境仍由 Spring 启动参数决定；错误的堆、线程池和连接池设置还可能引发 OOM、上下文切换、请求堆积或数据库连接竞争。

**根因**

混淆了 Maven 构建 Profile 和 Spring 运行 Profile，同时直接沿用课程示例的 6G JVM、20～200 业务线程和 5000 长队列，没有按机器资源和真实任务模型进行预算。

**定位过程与证据**

全仓检索确认 `java_jvm`、`profileActive` 仅出现在两个根 POM，没有消费者。market 当前每次营销试算会提交两个 `FutureTask` 并等待结果；mall 的自定义业务线程池当前没有对应的高并发计算路径，因此两者不应配置相同的大线程池。

**解决方案**

- 删除无效 Maven dev/test/prod Profile，规定 Maven 只负责构建，运行环境只由 `SPRING_PROFILES_ACTIVE=prod` 控制。
- 移除 Spring Profile 的 dev 默认值，漏配时立即启动失败。
- 当服务独占 2C4G 时可以使用较大堆，但实际上线为三个 Java 服务与 Redis、RabbitMQ、ES 共享一台 2C4G，因此 market/mall 使用 `Xmx=512MB`、容器上限 768MB；chat 使用 `Xmx=640MB`、容器上限 896MB；ES 堆 384MB、容器上限 768MB。
- 三个 Java 服务的 Tomcat 最大工作线程都收紧为 60，避免 2 核 CPU 上的过度线程竞争。
- Hikari 最大连接数分别为 market 8、mall 8、chat 6，单副本应用侧理论上限为 22；连接等待、校验、生命周期和保活均设置有界。
- 主机增加 2GB swap 作为突发保护，并按 Elasticsearch 要求将 `vm.max_map_count` 固化为 262144；swap 不作为常态内存使用。
- market 业务线程池为 4～8、队列 200；mall 为 2～4、队列 100；均使用 `CallerRunsPolicy` 形成背压。
- RabbitMQ 消费线程为 2～4、prefetch 为 1；Redisson 连接池缩小到 8，market 的 DCC Redis 独立池缩小到 4。

**验证结果**

- market 14、pay-mall 60 个单元测试全部通过，两个可执行 JAR 构建成功。
- Spring Boot 3.5.6 配置元数据确认 Tomcat 和 RabbitMQ 参数名称有效。
- 不提供 `SPRING_PROFILES_ACTIVE` 启动两个 JAR，均在读取配置阶段按预期失败，没有回退 dev。
- 生产服务器上 7 个容器全部运行，三个后端健康检查为 `UP`；一次稳定态快照中主机内存 3.5GB、已用 2.3GB、可用约 950MB，swap 已用约 105MB。
- RDS 实例的 `max_connections=2520`、`max_user_connections=2000`，当前三个单副本应用池上限 22，但仍需对运维、监控和后续副本预留连接。
- 尚未执行 2C4G 真实压测，当前数值是已通过空载实启的保守初始值，不是容量承诺。

**遗留风险**

当前已经是共享 2C4G 部署，ES 稳定态约占容器上限的 92%，主机也已出现少量 swap，说明资源余量有限。在未完成压测前不应扩大线程池或堆，也不应直接增加服务副本。最终值要结合 QPS、P95/P99、CPU、堆、GC、Hikari 等待、RDS 连接使用率和拒绝次数调整；流量增长时应优先将 ES 或 chat 拆到独立主机。

**面试回答**

> 我发现原项目把 Maven Profile 当成 Spring 运行环境开关，但相关属性没有被任何插件使用，而且生产 JVM 还配置了超过机器物理内存的 6G 堆。我把环境选择收口到运行时 `SPRING_PROFILES_ACTIVE`，让漏配直接失败；再根据 2C4G 和真实任务模型限制 JVM、Tomcat、Hikari、业务线程池与 MQ 消费并发。配置通过单测、打包和 fail-fast 启动验证，容量值则保留为压测后的可调参数。

### P38 生产首启时配置已读取但客户端或健康检查未真正生效

**发现日期**：2026-09-02  
**所属模块**：拼团 / 商城 / 客服 / 部署  
**当前状态**：已修复并通过服务器实启验证

**问题现象**

- market 和 mall 能读到 Redis 密码，但 Redisson 配置没有调用 `setPassword`，运行时报 `NOAUTH Authentication required`。
- 生产环境关闭支付宝后，`OrderService` 和未支付通知任务仍强制依赖 `AlipayClient`，导致 mall 整体无法启动。
- chat 自定义 ES 客户端使用容器地址，但 Spring Boot Actuator 健康检查仍连接 `localhost:9200`，导致实际业务客户端和健康状态不一致。

**根因**

配置属性存在不等于运行时组件已应用该值；可选功能的条件配置也必须覆盖所有依赖者，不能只关闭底层 Bean。

**解决方案**

- 两个 `RedisClientConfig` 在密码非空时显式调用 `SingleServerConfig#setPassword`。
- `OrderService` 将 `AlipayClient` 改为可选依赖，只在真正执行支付/退款时通过 `requireAlipayClient()` 做明确错误提示；`NoPayNotifyOrderJob` 也用 `alipay.enabled=true` 条件加载。
- chat 生产环境同时设置 `SPRING_ELASTICSEARCH_URIS=http://elasticsearch:9200`，让 Actuator 与业务客户端指向同一 ES。

**验证结果**

- 修复后 market、mall、chat 均能在 Redis 开启密码、支付关闭、ES 使用容器网络的生产组合下启动。
- 三个 Actuator 均返回 `UP`，商品接口读到真实 RDS 数据，客服会话接口能正常访问。
- 最终三个后端容器重启次数均为 0，最近日志未再出现上述启动错误。

**面试回答**

> 生产首启帮我发现，“配置文件有这个字段”不能证明客户端真正使用了它。我分别沿 Redisson 构建、支付 Bean 依赖和 Actuator 自动配置链路定位，将密码显式传给客户端、让支付依赖与开关一起条件加载，并统一业务与健康检查的 ES 地址。最后用实际密码中间件、关闭支付的启动组合和真实接口回放验证，而不是只看编译通过。

### P39 客服模型角色在同步接口不生效且默认深度思考导致前端超时

**发现日期**：2026-09-02  
**所属模块**：客服 / 部署  
**当前状态**：已修复并通过生产 HTTP 验证；知识文档尚未入库

**问题现象**

生产客服最初没有有效模型凭据，前端只能显示“暂时无法回答”。接入阿里云百炼业务空间后，普通问答可以成功，但知识库问题一次需要约 90 秒，超过前端 60 秒超时；同时配置中的 `RAG_CHAT_MODEL` 和 `COMMON_CHAT_MODEL` 只作用于流式接口，同步接口仍共用主 `chatModel`。

**影响范围**

商城悬浮客服和独立客服页都调用同步 `POST /api/v1/chat`。即使后端最终返回 200，浏览器也会先超时并展示失败提示；模型角色配置名义上存在，实际调用链却没有使用。

**根因**

- `CommonChatAiService` 和同步 RAG 的动态 AI Service 都注入主 `chatModel`，没有独立的同步 `commonChatModel`、`ragChatModel` Bean。
- Qwen3.5/Qwen3.6 默认启用思考模式，客服链路还包含意图识别、查询改写和最终生成，多次模型调用叠加后超过前端超时。
- 截图中的 `qwen-max-latest` 在当前业务空间返回 `model_not_found`，不能直接照抄模型清单。

**定位过程与证据**

- 使用新业务空间 Key 做最小真实调用：`qwen3.5-flash`、`qwen3.6-plus` 和 `text-embedding-v4` 返回 200，Embedding 实际为 1536 维；`qwen-max-latest` 返回 404。
- 沿同步 Controller → `ChatApplicationService#chat` → `CommonChatAiService`/动态 RAG AI Service 检查 Bean，确认角色模型只在流式路径生效。
- 对同一模型传入 `enable_thinking=false` 后，单次直接调用降到亚秒级，因此将该参数做成生产环境变量，而不是只放宽浏览器超时。

**解决方案**

- 新增同步 `ragChatModel`、`commonChatModel` Bean，并在同步 RAG 和普通客服调用链按限定名注入。
- 生产模型分工为：`qwen3.5-flash` 负责意图识别、查询改写和标题，`qwen3.6-plus` 负责普通回答和 RAG 生成，`text-embedding-v4` 使用 1536 维向量。
- 所有文本聊天模型统一通过 `MODEL_ENABLE_THINKING=false` 传递 `enable_thinking`；视觉模型不附带该参数。
- API Key 仅写入服务器权限为 `600` 的 `chat.env`，没有进入 Git、JAR 或部署示例，上传到服务器的临时 CSV 已删除。

**验证结果**

- 客服模块 60 个单元测试全部通过，可执行 JAR 构建成功，服务器 JAR SHA-256 与本地一致。
- 生产容器健康检查为 `UP`，重启次数为 0，启动日志无 `ERROR`、`APPLICATION FAILED` 或 `ConnectException`。
- 经公网 HTTPS 实测，普通客服约 4.07 秒返回 200，知识库问题约 4.77 秒返回 200，均低于前端 60 秒超时；落库的助手消息模型名均为 `qwen3.6-plus`。
- Elasticsearch 已创建 1536 维版本化索引并校验 mapping，但当前文档数为 0，因此产品问题会保守回答“现有知识库无法确认”，引用列表为空。

**遗留风险**

模型链路和空知识库兜底已经真实联调，但产品知识还没有通过 OSS/MinerU 上传、解析、Embedding 和发布形成完整 E2E 证据。正式提供产品答案前必须完成文档入库，并用真实问答集评估召回、引用和幻觉；当前业务空间 Key 已在本地文件和对话上下文中出现，仍建议在部署验收后轮换。

**面试回答**

> 我接入模型后没有停在“接口返回 200”，而是发现同步客服仍错误复用主模型，而且 Qwen 默认思考让多段链路超过前端 60 秒超时。我按职责拆出同步 RAG 和通用模型 Bean，并把 `enable_thinking=false` 作为生产参数统一传给文本模型。最终用公网接口、数据库模型名、容器日志和真实响应时间验证，延迟从约 90 秒降到约 5 秒；知识索引为空的边界则明确保留为未完成项。

### P40 全量知识文档入库时大图、超多图片和型号误召回导致部分资料不可用

**发现日期**：2026-09-03  
**所属模块**：客服 / 知识库 / 部署  
**当前状态**：已解决并完成生产 E2E 验证

**问题现象**

将 `file/知识库文档` 的真实资料批量导入后，部分超长产品详情图被 MinerU 的 10MB 单文件限制拒绝，一本产品手册因解析结果包含 433 张图片而触发代码内固定 100 张上限。文档发布后，询问明确型号 `TL-7DR6560` 时又召回了多个其他 `TL-*` 型号，最终客服回答“现有知识库无法确认”。

**影响范围**

超限文档无法进入解析、分片和索引阶段；型号误召回会污染最终 RAG 上下文，即使正确资料已经入库，模型也可能因证据冲突拒绝回答。

**根因**

- `MineruArchiveParser` 使用固定图片数量阈值，外部 `CHAT_MAX_IMAGES_PER_DOCUMENT` 只约束后续图片处理，无法控制 ZIP 解析阶段。
- MinerU API 对单个上传文件有 10MB 限制，应用自身的 50MB 上传限制不能覆盖第三方约束。
- ES 中 `metadata.productModel` 是 `text + keyword` 多字段，全文检索却在 `text` 字段上使用 `match` 过滤；`TL-7DR6560` 被拆词后会因共同的 `TL` 命中其他型号。

**解决方案**

- ZIP 解析器和图片处理阶段统一读取 `group-chat.image.max-images-per-document`，生产环境按真实手册设置为 500；该手册 433 张图片的解压体积约 16MB，仍受 200MB 解压总量保护，适合当前 2C4G 主机。
- 不修改原始资料，将 4 张超过 10MB 的详情长图缩放压缩为约 2.5～3MB 的适配副本后重新入库；原始文件继续保留。
- 将产品型号过滤改为 `metadata.productModel.keyword` 的 `term` 精确匹配，阻止不同 `TL-*` 型号互相污染召回结果。
- 3 张没有任何可提取文字的纯商品外观图不伪造文本分片；对应型号的产品介绍、安装指南和详情图均已发布。

**验证结果**

- 客服模块 60 个单元测试通过，可执行 JAR 构建成功，本地与服务器 SHA-256 一致；生产健康检查为 `UP`，容器重启次数为 0。
- 80 个源文件中有 79 个不同 SHA-256；最终形成 76 条已发布知识文档记录、839 个 MySQL 分片和 1087 张知识图片，其中 729 个可搜索分片与 ES `_count` 完全一致。
- 数据库仍保留 7 条失败记录用于审计：4 条是被压缩适配版替代的超限原图，3 条是无可索引文字的纯外观图。
- 公网 HTTPS 实测 `TL-7DR6560 有几个 2.5G 网口？` 返回 200，并依据精确型号资料回答“4个”；操作类问题“如何查看连接无线路由器的终端数量？”也返回完整步骤和引用来源。

**遗留风险**

生产凭据曾在本地文件和对话中出现，验收后仍需在阿里云、MinerU 和模型平台轮换。纯图片若未来需要支持以图搜图，应单独建设视觉描述或多模态向量索引，而不是写入无依据的占位文本。

**面试回答**

> 我用真实的近 300MB 产品资料做全量入库时，发现第三方 10MB 限制、代码固定图片上限和 ES 型号分词三个独立问题。我把图片上限统一外部化，按 2C4G 的真实解压体积设置为 500，并为超长详情图生成不改变原件的上传适配副本。随后把型号过滤从分词 `match` 改成 `.keyword` 的精确 `term`。最后用 MySQL 分片数、ES 可搜索文档数和公网客服问答三层证据验证了完整链路。

### P41 后端已支持 SSE，但商城客服页面仍然一次性显示完整回答

**发现日期**：2026-09-03  
**所属模块**：商城前端 / 客服 / SSE  
**当前状态**：已解决、已上线并完成生产浏览器验证

**问题现象**

客服页在模型生成期间只显示等待动画，必须等整个 RAG 链路完成后才一次性出现回答，用户会误以为客服卡死或超时。

**根因**

客服后端已提供 `POST /api/v1/chat/stream`，并按 `PROGRESS → REFERENCE → ANSWER... → COMPLETE` 返回 SSE；Nginx 也已关闭代理缓冲。但商城悬浮客服和独立客服页仍调用同步 `POST /api/v1/chat`，前端没有消费 SSE 事件。

**解决方案**

- 参考 `know-engine` 的 `Flux + SSE` 链路，但复用当前项目已有的结构化事件协议，不引入另一套字符串标记。
- 由于接口为 POST，不使用只支持 GET 的原生 `EventSource`，而是使用 `fetch + ReadableStream + TextDecoder` 增量解析 SSE，并正确处理网络分片跨越 JSON 边界的情况。
- 两个客服入口在原位创建助手消息：进度事件更新提示，引用事件先展示资料，回答事件逐段追加文字，完成事件校准最终答案。
- 将后端 `conversationId` 与浏览器会话一起保存，后续问题复用同一服务端多轮记忆；短期 OSS 签名 URL 仍不持久化。
- 保留同步接口作为兼容路径，流式空回答、服务端 `ERROR`、连接中断和 180 秒无数据超时均有明确回退文案。

**验证结果**

- 前端 18 项测试全部通过，包含 SSE 拆分为任意网络分片、回答分片回调和 `ERROR` 事件转换；Vite 生产构建成功。
- 客服 `ChatApplicationServiceTest` 3 项定向测试通过，其中已覆盖流式 RAG 的进度、引用、文本分片和完成事件。
- 公网 HTTPS 实测可持续收到多个 `ANSWER` 事件；真实浏览器先显示意图识别和检索进度，再显示引用卡片和逐段答案，完成后输入框恢复可用。

**遗留风险**

当前会话仍以浏览器生成的匿名 `clientId` 隔离，没有与商城账号绑定，因此无法跨设备恢复；生产容量与高并发断流恢复仍需单独压测。

**面试回答**

> 我排查“客服不流式”时没有直接重写后端，而是先对比参考项目和当前调用链。最终发现后端和 Nginx 已经正确支持 SSE，真正缺口是前端还在调同步接口。我用 `fetch + ReadableStream` 实现 POST SSE 解析，将进度、引用、token 和完成事件映射到同一条助手消息，并保存服务端会话 ID 续接多轮上下文。最后用分片 SSE 单测、后端事件单测和公网浏览器逐阶段显示三层证据完成验证。

### P42 登录成功后前端缺少明确状态，重新打开账号窗口仍容易出现登录表单

**发现日期**：2026-09-03  
**所属模块**：商城前端 / 账号登录  
**当前状态**：已解决并完成生产静态资源部署

**问题现象**

登录成功后导航只把“登录”改成含义不明显的“账号”，用户无法一眼确认当前是否已登录。登录状态又分散在 Token、userId 和 Vue 页面内存中，账号窗口打开时不会主动重新读取本机会话，容易出现页面状态与存储不一致。

**解决方案**

- 在 `mallSession.js` 统一封装 Token、JWT `sub/exp/authType`、userId 和账号名的存储、恢复与清理，不再由页面分散写入。
- 页面首次载入、打开账号窗口、重新可见及其他标签页修改 `localStorage` 时重新同步会话；JWT 过期或后端返回 401/1006 时仍统一清理。
- 导航按钮改为带状态点的“已登录”；账号窗口默认展示“当前已登录”、账号名、登录方式和有效时间。
- 将“查看我的订单”、“修改密码”和“退出当前账号”作为明确操作，密码表单只在用户主动点击“修改密码”后出现。

**验证结果**

- 前端 19 项测试全部通过，新增用例覆盖账号名、登录方式、Token 和 userId 的整体存储/恢复，以及退出时一次性清理。
- Vite 生产构建成功，公网首页已引用本次新构建的 JS/CSS 指纹资源。

**验证边界**

本次没有获取或新建额外的生产商城账号，因此没有为了 UI 验证污染生产用户表；已登录分支通过 JWT 恢复单测和 Vue 生产编译验证。

**面试回答**

> 我没有只把导航文案从“账号”改成“已登录”，而是先统一了 JWT 会话的读写边界：前端用 `sub` 确定用户、用 `exp` 判断过期，并在打开账号窗口、切回页面和跨标签页变化时重新同步。然后把登录态设计成账号概览，先告诉用户“当前已登录”和有效期，再提供订单、修改密码和退出操作。这样同时解决了状态不一致和交互表达不清两个问题。

### P43 客服长回答无法滚动到底部且 Markdown 标记直接显示

**发现日期**：2026-09-03  
**所属模块**：商城前端 / 智能客服 / Markdown / 响应式布局  
**当前状态**：已解决、已上线并完成生产浏览器验证

**问题现象**

客服回答较长时，消息内容会超过可视区域，但页面本身被设置为 `overflow: hidden`，用户无法稳定下拉到回答底部。同时回答中的 `###`、`**` 和列表星号被直接显示，标题、加粗和列表均没有排版效果。

**根因**

- 独立客服使用“固定视口页面 → 固定高度工作区 → Grid 聊天区 → 消息区”的嵌套布局，但 Grid 子项 `.support-chat-area` 缺少 `min-height: 0` 和自身的溢出约束，内容最小高度可以撑开父级；悬浮客服的 Flex 消息区也缺少相同约束。
- 两个客服组件都使用 `{{ message.text }}` 把模型回答渲染为纯文本，浏览器不会主动解释 Markdown，所以模型输出的语法标记会原样出现。

**解决方案**

- 将页面明确限制为 `100dvh`，聊天区补齐 `height: 100%`、`min-height: 0` 和 `overflow: hidden`，只让 `.support-messages` / `.chat-body` 使用 `overflow-y: auto`；增加触控惯性、滚动链隔离和稳定滚动条空间。
- 消息区域增加可聚焦语义，除鼠标滚轮和触控外，也可通过 Home、End、PageUp、PageDown 等键盘操作浏览长回答。
- 抽取共享 `MarkdownMessage` 组件和 `renderMarkdown` 工具，独立客服与悬浮客服统一支持标题、粗体、列表、引用、链接、代码块和表格。
- Markdown 解析器关闭原始 HTML，并拒绝 `javascript:` 等危险链接；安全外链统一使用新窗口及 `noopener noreferrer`，避免把模型内容直接作为可执行 HTML。

**验证结果**

- 前端 22 项测试全部通过，新增用例覆盖标题、粗体、列表、原始脚本转义、危险链接拒绝和安全外链属性；Vite 生产构建成功，`npm audit` 为 0 个已知漏洞。
- 本地浏览器构造长会话后，消息区可视高度 818px、内容高度 2909px，可从 `scrollTop=0` 滚动到最大值 2091；文档页面高度没有随消息内容增长。
- 公网真实客服回答已渲染出 8 个列表和 21 处加粗内容，页面可从顶部滚动到长回答底部，原始 `**` / `###` 不再可见，控制台无错误。

**遗留风险**

Markdown 在 SSE 流式生成期间会随片段重复解析；当前回答长度和 500 字提问限制下性能正常。若以后允许超长代码或超大表格输出，应增加回答长度上限或虚拟列表，而不是继续放大单条消息 DOM。

**面试回答**

> 我排查客服长回答时发现不是滚动条颜色问题，而是 Grid 和 Flex 子项默认最小高度让内容撑开了固定视口，外层又禁止页面滚动，最终形成裁切。我补齐 `min-height: 0` 与独立 `overflow-y: auto` 的高度链，并增加键盘滚动语义。Markdown 部分抽成共享安全渲染组件，关闭原始 HTML 和危险协议。最后通过单测、构建、本地长会话尺寸测量和公网真实 RAG 回答验证了列表、加粗与首尾滚动。

### P44 支付未配置时仍先执行拼团锁单

**发现日期**：2026-09-03  
**所属模块**：支付商城 / 拼团锁单 / 支付宝 / 商城前端  
**当前状态**：已解决并上线；支付宝沙箱付款、异步回调验签、订单落库和拼团结算已完成 E2E 验证

**问题现象**

用户选择拼团并提交订单后，页面只提示“订单暂时没有提交成功，请稍后重试”。生产日志显示商城已成功调用拼团服务锁定名额并生成营销订单，之后才因支付宝客户端不可用而抛出“支付功能尚未配置”。这会让用户看到下单失败，但营销侧仍可能保留一个未支付锁单。

**根因**

- 生产环境明确设置了 `ALIPAY_ENABLED=false`，同时应用 ID、商户私钥和支付宝公钥为空，因此没有创建 `AlipayClient`。
- 原下单顺序为：保存商城 `CREATE` 订单 → 查询并锁定拼团名额 → 保存营销价格快照 → 创建支付宝预支付单。支付宝客户端检查位于最后一步，失败发现得过晚。
- 前端把所有异常统一收敛成普通重试提示，无法告诉用户当前并不是瞬时网络故障。

**生产证据**

- 订单 `3159740848508812` 的拼团锁单已成功，营销订单号为 `716337334871`、支付金额为 235 元，随后在 `OrderService.requireAlipayClient` 失败；该笔之后走退款解锁链路恢复。
- 订单 `3576042769900356` 同样先锁单成功，生成队伍 `75069365`、支付金额为 185 元，之后因相同原因失败。排查时该队伍仍显示 `lockCount=1`，未擅自对生产业务数据执行人工退款。

**解决方案**

- 在 `OrderService.createOrder` 的最入口调用 `requireAlipayClient()`，只有支付客户端可用时才进入订单查询、落库、商品查询和营销锁单流程。
- 增加回归测试，把支付宝客户端置空后验证返回“支付功能尚未配置”，并验证订单仓储、商品端口和拼团端口均没有发生交互。
- 前端只对白名单异常“支付功能尚未配置”展示“支付功能暂未开通，请稍后再试”；其他后端异常继续使用统一提示，避免泄露内部错误。
- 重新构建商城 JAR 和 Vue 静态资源，仅重启商城容器；部署前保留旧 JAR 备份，并核对服务器制品哈希。
- 沙箱真实付款后发现容器默认时区为 UTC，而支付宝 `gmt_payment` 是中国标准时间；原代码依赖 JVM 默认时区，导致库内支付时间被推后 8 小时，拼团有效期校验误判为过期。`AliPayController.parsePayTime` 现显式使用 `Asia/Shanghai`，并增加 JVM 默认为 UTC 时的回归测试。

**验证结果**

- 商城后端 62 项测试全部通过；新增用例分别证明支付未配置时不会发生本地写入/远程锁单，以及 UTC 容器中支付宝时间仍解析为正确瞬时。
- 商城前端 24 项测试和 Vite 生产构建通过；新增用例覆盖明确提示与未知异常收敛。
- 新商城 JAR 与服务器文件 SHA-256 一致，商城容器以 `prod` 启动，公网 `/mall-api/actuator/health` 返回 `UP`，公网首页已加载本次新前端指纹资源。
- 2026-09-03 将支付宝沙箱 APPID、PKCS8 应用私钥、支付宝公钥、沙箱网关和 HTTPS 回调地址通过服务器环境变量注入；私钥推导出的公钥指纹与平台应用公钥一致，应用私钥和支付宝公钥没有混用。
- 在 RDS 中备份后清理过期拼团队伍、成员、通知任务和未完成历史商城订单，保留已支付历史样本；8 个测试商品及活动统一延长到 2030-12-31，补齐 20/30/50 元三档优惠并清除相关 Redis 缓存。
- 公网真实调用完成账号注册、商品活动查询、商城订单创建和营销锁单。订单 `3344789001785376` 的商品原价为 325 元、优惠 30 元，营销价格快照、商城 `pay_amount` 与支付宝沙箱表单均为 295 元；表单指向沙箱网关且使用正确 APPID。
- 获得明确授权后，使用支付宝沙箱买家完成 295 元模拟付款，页面返回付款成功；服务器收到交易号 `2026090322001420530510030274` 的异步通知并验签成功，商城订单 `3344789001785376` 从 `PAY_WAIT` 进入 `PAY_SUCCESS`。
- 修复时区并对受影响的这一条沙箱订单做备份和定点校正后，结算补偿任务自动成功：营销成员订单 `762822502803` 状态变为已完成，`out_trade_time` 为北京时间 `2026-09-03 11:27:02`，队伍 `70725360` 的 `complete_count=1`、`lock_count=1`；新商城 JAR 健康检查为 `UP`，部署前 JAR 与测试订单均有服务器备份。

**验证边界与后续动作**

当前已完成单个买家的“预支付 → 沙箱付款 → 异步通知验签 → 商城支付成功 → 拼团成员结算”真实 E2E。该队伍目标人数为 2，当前只有 1 名已支付成员，所以队伍保持进行中且没有生成成团通知属于预期结果；第二位买家参团后的成团通知，以及退款/解锁链路，仍应作为独立场景继续联调，不能由本次单人付款结果替代。

**面试回答**

> 这次表面上是拼团锁单报错，但生产日志证明锁单已经成功，真正异常发生在后续创建支付宝预支付单。根因是外部能力校验放得太晚：应用先写本地订单、再锁远程名额，最后才发现支付客户端没有初始化。我把支付能力检查前移到下单入口，并用无交互测试证明失败时仓储、商品和营销端口都不会被调用；前端同时给出明确但不泄露内部细节的提示。这样即使生产关闭支付，也不会再制造新的无效锁单。

### P45 客服推荐问题命中质量不稳定且资料编号反复打断阅读

**发现日期**：2026-09-04
**所属模块**：智能客服 / RAG 提示词 / 商城前端
**当前状态**：已解决并上线；前端 25 项、客服后端 61 项测试及公网真实问答验证通过

**问题现象**

- 客服首页推荐的“路由器上网慢应该怎么排查？”容易召回家庭路由器、企业网络、双 WAN 等多类资料，回答很长且主题发散；“怎么查看已经连接的终端？”也会一次罗列多个入口。
- 模型在几乎每条步骤后都输出 `[资料1]`，而界面下方已经单独展示资料卡片，重复编号明显影响连续阅读。

**根因**

- 推荐问题最初按业务覆盖面选择，没有用生产知识库的真实召回与回答结果反向筛选。
- 七类业务提示词都要求“每个事实后标注 `[资料N]`”，模型严格执行后形成密集重复标记。
- 历史对话会从浏览器本地存储恢复，即使只修改提示词，旧回答中的资料编号仍会继续显示。

**解决方案**

- 对六个候选问题逐个调用生产客服接口，依据准确性、简洁度和资料命中情况，最终保留“TL-7DR6560 有几个 2.5G 网口？”、“忘记 Wi-Fi 密码怎么办？”、“两台易展路由器怎么组网？”和“路由器怎么恢复出厂设置？”。
- 在统一的 `PromptService` 输出规范中明确：先直接回答，再给必要步骤；正文不插入资料编号，来源统一由页面下方资料卡片展示。
- 在共享 Markdown 渲染入口增加兼容清理，仅移除 `[资料N]` 标记，因此新回答、历史对话、独立客服页和悬浮客服都能同时生效，原有资料卡片数据不受影响。

**验证结果**

- 新增前端回归用例，验证多个 `[资料N]` 标记会被移除且自然标点保留；前端 25 项测试与 Vite 生产构建通过。
- 新增后端提示词回归用例，遍历全部客服意图，确保提示词不再要求行内资料编号且明确保留资料卡片；客服后端 61 项测试和生产 JAR 构建通过。
- 新 JAR 与服务器文件 SHA-256 一致，聊天服务使用 `prod` 启动，公网健康检查返回 `UP`；公网首页已加载新前端指纹资源。
- 公网浏览器中四个新推荐问题均已展示。点击型号问题后，回答正确显示“4个”，正文没有 `[资料1]`，下方仍正常展示 5 张资料卡片及相关图片。

**面试回答**

> 我没有只凭感觉改客服推荐词，而是把候选问题逐个跑生产知识库，根据答案准确度、长度和召回聚焦程度筛选出四个稳定问题。资料编号重复的根因在提示词：模型被要求在每个事实后加引用，但前端已经有独立资料卡片。我把来源展示职责统一交给资料卡片，同时在共享 Markdown 渲染入口兼容清理历史回答中的编号，最后用前后端测试和公网真实问答确认正文更易读且来源没有丢失。

### P46 悬浮客服尺寸偏小且知识库页面视觉体系与客服割裂

**发现日期**：2026-09-04
**所属模块**：商城前端 / 悬浮客服 / 知识库管理
**当前状态**：已解决并上线；前端 25 项测试、生产构建及本地/公网浏览器验证通过

**问题现象**

- 商城右下角“问一问”入口和展开后的客服窗口偏小，在桌面大屏上阅读长回答、查看资料和输入问题时空间不足。
- 知识库登录及管理页面使用浅绿色光晕、网格背景和超大标题，而独立智能客服使用白灰底、细边框和深色图标，两套页面像来自不同产品。

**根因**

- 商城页面存在一组后置 CSS 覆盖，把原本较大的客服窗口固定缩小为 `520 × 640px`，同时把入口缩为 `128 × 56px`。
- 知识库页面的样式是单独设计的，没有复用客服界面的颜色、边框、圆角、顶栏高度和按钮层级。

**解决方案**

- 将桌面端“问一问”入口提升到 `156 × 66px`，展开窗口提升到最大 `680 × 780px`，头像、文字、推荐问题和输入区同步按比例放大；高度仍受视口约束，避免小屏溢出。
- 移动端保留单独尺寸和 `100dvh` 高度约束，确保底部输入框、发送按钮及关闭入口始终可触达。
- 将知识库登录页和管理页统一为客服页的白灰背景、58px 顶栏、深色主按钮、细灰边框及克制阴影，并保留上传、检索、状态展示等原有功能。

**验证结果**

- 前端 25 项测试全部通过，Vite 生产构建成功，`git diff --check` 无格式问题。
- 在 1280 × 720 浏览器视口实测，客服窗口宽度为 680px，高度自动收缩为 614px，窗口、输入区和底部关闭按钮完整位于视口内。
- 知识库登录页在相同视口下无横向溢出，导航、登录卡片和输入区均正确展示新的统一视觉风格。
- 新静态资源部署后，公网首页加载 `index-CMyIhd-V.js` 与 `index-BVVdhmrp.css`；公网浏览器复测尺寸与本地一致，商城、客服和 Nginx 等线上容器均保持运行。

**面试回答**

> 这次不是单纯把弹窗写死放大。我先定位到商城页面后置样式把原始窗口覆盖成了 520 × 640px，再把入口、窗口和内部排版整体提升两个尺寸等级，同时用视口高度约束保证小屏可用。知识库页面则统一到客服已有的白灰、细边框和深色控件体系，最后通过自动化测试、生产构建和真实浏览器尺寸检查确认没有引入溢出。

### P47 首页商品卡片只显示日常价，没有展示已有活动价

**发现日期**：2026-09-04
**所属模块**：商城前端 / 商品查询 / 拼团活动试算
**当前状态**：已解决并上线；前端 27 项、拼团后端 16 项测试、生产构建及公网验证通过

**问题现象**

- 商品详情抽屉可以显示拼团优惠，但首页 8 张商品卡片全部显示“单独购买”和日常价，用户必须逐个点开才能知道是否有活动。
- 生产接口核对显示，商品列表的 `groupBuyMarket` 全部为 `null`，而同一商品详情能返回活动 ID、原价、优惠金额和活动到手价。

**根因**

- 首页只调用轻量商品列表接口，该接口没有请求拼团营销试算；`ProductGrid` 虽然已经具备活动价和原价划线的渲染逻辑，但始终拿不到活动数据。
- 首页并行补查详情后又暴露出第二个问题：营销查询的限流键只有 `userId`。同一用户同时查询 8 个商品时只有第一个通过，其余请求被当作超频，并可能累计进入 24 小时黑名单。

**解决方案**

- 首页取得基础商品后，使用当前登录用户 ID；未登录时使用浏览器稳定游客 ID，并行查询最多 8 个可见商品的详情。
- 将详情中的 `groupBuyMarket` 合并回商品卡片：有有效活动时展示“活动价”、优惠后价格和划线日常价，同时标记为多人拼；没有活动时仍只展示日常价。
- 每个详情请求独立降级，单个营销试算超时或失败只影响该商品的活动价，不会导致整个商品列表加载失败。
- 扩展通用限流切面支持组合字段，将该只读接口的限流键改为 `userId + goodsId`，并取消失败后的用户黑名单累计。不同商品可并行试算，同一用户重复请求同一商品仍受每秒限流。

**验证结果**

- 新增两个前端用例，覆盖活动商品补全价格和单商品营销请求失败降级；前端测试由 25 项增加到 27 项并全部通过。
- 新增组合限流键回归测试，并验证原单字段限流键仍兼容；拼团后端 16 项测试全部通过。
- Vite 与拼团 JAR 生产构建成功，最终 JAR 未包含本地和生产密钥配置，`git diff --check` 无格式问题。
- 新前端加载 `index-BN5onlDa.js`，新拼团 JAR 与服务器 SHA-256 一致，market 容器使用 `prod` 启动且内部健康检查为 `UP`。
- 使用同一用户连续查询 8 个商品，全部返回活动 ID `2026072901` 至 `2026072908`，活动价分别为 185、259、295、225、639、355、555、318 元；公网浏览器中 8 张卡片均展示活动价和划线日常价，页面无横向溢出。

**面试回答**

> 这次页面不是不会渲染活动价，而是列表接口没有活动数据。我复用了已经完成真实锁单验证的商品详情试算接口，对首页最多 8 个可见商品并行补全活动信息，并采用单商品失败单独降级的策略。这样有活动就显示活动到手价和划线原价，营销服务局部异常时仍能展示基础商品和日常价。

## 9. 当前仍需诚实说明的遗留风险

以下问题已经识别，但当前不能在面试中说成全部解决：

1. 拼团 lock/settlement/refund 和 DCC 更新接口需要明确的服务间认证。
2. 微信 scene/ticket/openid 缓存使用 Guava，不能直接支持多实例，应迁移 Redis。
3. 客服 MinerU 轮询缺少多实例任务抢占或分布式锁。
4. MySQL、ES、OSS 之间不存在分布式事务，删除和重建索引仍需任务化补偿。
5. 管理员 Token 当前能力较轻，仍缺少完善的过期、登出、多会话和权限模型。
6. 生产配置已经改为环境变量注入并排除本地 profile；曾经使用过的外部凭据仍需在各平台控制台完成轮换。
7. 跨服务支付、MQ 退款、微信、支付宝、OSS、MinerU、ES 和模型仍需保留真实联调证据。
8. 多数商城/拼团业务表缺少当前权威 DDL，生产约束必须在真实库复核。
9. 客服已启用 BM25 + 向量混合检索并完成两条生产问答 E2E，但仍缺少成规模真实问答集的 Recall@K、引用准确率和幻觉率评估，本地 Rerank 仍未启用。
10. 1536 维 ES 索引已完成全量资料入库验证；仍有 3 张无可提取文字的纯外观图不能形成文本分片，后续若要求图片语义检索需建设多模态索引。
11. 客服已续接服务端多轮上下文，但会话仍按浏览器匿名 `clientId` 隔离，尚未与商城账号绑定，不能跨设备同步。

## 10. 面试时可直接使用的总结版本

### 30 秒版本

> 项目里最有代表性的难点有三类。第一是支付和拼团跨服务一致性，我用数据库状态机、幂等业务号、本地消息表和定时补偿处理重复回调、结算失败和退款不确定状态。第二是微信登录和用户鉴权，我用 scene 与 ticket 双向绑定防止二维码串号，并以 JWT subject 作为订单接口的权威用户身份。第三是 RAG 文档质量，我没有只调切块参数，而是加入 Unicode/OCR 规范化、HTML 表格结构化、Markdown 块切分、父子分片、型号过滤、质量门禁和版本化重建索引。

### 两分钟版本

> 商城和拼团是两个独立服务。商城先生成订单，并用同一个 orderId 调拼团锁单，保存营销价格快照后再创建支付单。支付回调通过验签和 `PAY_WAIT → PAY_SUCCESS` 条件更新保证幂等，支付事实先落 MySQL，远程拼团结算失败由定时任务补偿。拼团结算在成员和队伍两级做条件更新，避免重复计数；成团通知使用 notify_task 本地消息表和数据库条件抢占。退款则引入 WAIT_REFUND 中间态，使用稳定退款号，先查询支付宝再重试，只有明确退款成功才关闭订单。
>
> 登录方面，ticket 只是短期二维码凭证，openid 才是同公众号下稳定用户标识。我增加了 sceneStr 与 ticket 双向绑定，防止不同浏览器串号，并且订单查询只相信 JWT subject。前端如果收到 401 或 1006，会清理过期会话并重新登录。
>
> 智能客服方面，MinerU 采用异步任务，原文件和 Markdown 保存在私有 OSS。针对真实分片中出现的 OCR 异体字、HTML 表格被截断和标题-only 块，我增加 NFKC/OCR 预处理、表格键值化、Markdown 块切分、小块合并和质量校验；Embedding 加入型号、标题、章节，明确型号的问题再做 ES metadata 过滤。长文档使用子块检索、父块回答，并通过处理版本和 reindex 接口支持规则升级。

## 11. 以后新增问题的固定记录模板

以后每解决一个新问题，复制下面模板追加到相应章节，并同步更新“问题速查表”和“遗留风险”：

```markdown
### PXX 问题名称

**发现日期**：YYYY-MM-DD  
**所属模块**：商城 / 拼团 / 前端 / 客服 / 数据库 / 部署  
**当前状态**：已解决并验证 / 已实现未联调 / 遗留风险

**问题现象**

用户看到什么、日志或数据表现是什么，不要只写“有 bug”。

**影响范围**

影响哪些接口、表、状态、用户或外部服务。

**根因**

说明技术根因和为什么原实现会出错。

**定位过程与证据**

关键日志、调用链、SQL、HTTP、源码类和验证命令。

**解决方案**

说明状态机、幂等键、事务边界、缓存/MQ、代码修改和取舍。

**验证结果**

分别写单测、构建、HTTP、数据库、Redis/MQ、第三方平台是否验证。

**遗留风险**

尚未覆盖的并发、多实例、数据迁移、安全或监控问题。

**面试回答**

用 3～6 句话说清问题、根因、方案和效果。
```

## 12. 关联项目文档

- [`接口文档.md`](接口文档.md)：真实 Controller、DTO、回调、Job、MQ 与跨服务接口。
- [`数据库表结构文档.md`](数据库表结构文档.md)：表职责、字段证据等级和业务关联。
- [`ekko-group-chat/README.md`](ekko-group-chat/README.md)：智能客服入库、检索、状态和部署说明。
- [`ekko-group-buy-market/README.md`](ekko-group-buy-market/README.md)：拼团营销服务说明。
- [`ekko-pay-mall/README.md`](ekko-pay-mall/README.md)：商城支付服务说明。
- [`ekko-group-buy-web/README.md`](ekko-group-buy-web/README.md)：Vue 商城功能和接口边界。
