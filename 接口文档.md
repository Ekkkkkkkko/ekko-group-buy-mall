# 当前项目接口文档

> 更新时间：2026-08-06  
> 扫描范围：`ekko-group-buy-market`、`ekko-pay-mall`、`ekko-group-chat`，并用 `ekko-group-buy-web` 的调用代码交叉核对。  
> 本文以当前工作区 Controller、DTO、Listener、Job 和 Retrofit 接口为准；静态源码不能证明服务、数据库、Redis、RabbitMQ、微信、支付宝、OSS、MinerU或Elasticsearch已经联通。

## 1. 服务与端口

| 服务 | 默认端口 | 职责 | Controller HTTP入口数 |
|---|---:|---|---:|
| `ekko-group-buy-market` | 8091 | 拼团试算、锁单、结算、退单、回调任务 | 6 |
| `ekko-pay-mall` | 8092 | 商品、登录、商城订单、支付、退款、微信/支付宝回调 | 13 |
| `ekko-group-chat` | 8095 | RAG问答、管理员登录、知识文档管理 | 5 |

共识别 **24 个Controller HTTP入口**。`ekko-group-chat` 另外暴露 `/actuator/health` 和 `/actuator/info` 两个运维端点。

## 2. 通用约定

### 2.1 拼团营销和支付商城响应

大部分业务接口返回：

```json
{
  "code": "0000",
  "info": "成功",
  "data": {}
}
```

- `code = 0000` 才表示业务成功。
- 两个服务的Controller多数会捕获异常并返回上述JSON，因此HTTP状态可能仍为200，调用方不能只判断HTTP 200。
- 支付宝、微信、拼团通知等回调接口返回裸字符串或XML，不使用统一JSON包装。

### 2.2 RAG客服响应

`ekko-group-chat` 直接返回业务对象；异常由全局处理器映射为HTTP 400/401/404/500/502：

```json
{
  "timestamp": "2026-08-02T12:00:00+08:00",
  "status": 401,
  "error": "Unauthorized",
  "message": "管理员登录已失效，请重新登录",
  "path": "/api/v1/documents/1"
}
```

### 2.3 鉴权类型

| 类型 | 使用位置 | 格式 |
|---|---|---|
| 商城用户JWT | 创建支付单、订单列表、退单 | `Authorization: Bearer <jwt>` |
| 客服管理员Token | 文档上传、查询、删除 | `Authorization: Bearer <admin-token>` |
| 拼团回调Token | 拼团服务调用商城成团回调 | `X-Group-Buy-Token: <token>` |
| 微信签名 | 微信公众号GET/POST回调 | `signature + timestamp + nonce` |
| 支付宝签名 | 支付宝异步支付回调 | 表单中的`sign`等参数 |

---

## 3. HTTP接口总览

### 3.1 拼团营销服务（8091）

| 方法 | 路径 | 功能 | 鉴权/限制 |
|---|---|---|---|
| POST | `/api/v1/gbm/index/query_ekko_group_buy_market_config` | 查询商品拼团优惠、队伍和统计 | 按`userId`限流；未见身份认证 |
| POST | `/api/v1/gbm/trade/lock_market_pay_order` | 锁定拼团名额和价格快照 | 未见服务身份认证；回调URL有白名单 |
| POST | `/api/v1/gbm/trade/settlement_market_pay_order` | 支付成功后结算拼团成员 | 未见服务身份认证 |
| POST | `/api/v1/gbm/trade/refund_market_pay_order` | 营销侧退单 | 未见服务身份认证 |
| GET | `/api/v1/gbm/dcc/update_config` | 发布动态配置变更 | 当前未鉴权，高风险管理接口 |
| POST | `/api/v1/test/group_buy_notify` | 模拟第三方接收成团通知 | 测试接口，固定返回`success` |

### 3.2 支付商城服务（8092）

| 方法 | 路径 | 功能 | 鉴权/限制 |
|---|---|---|---|
| GET | `/api/v1/products` | 查询上架商品列表 | 公开 |
| GET | `/api/v1/products/{productId}` | 查询商品和拼团详情 | 公开；`userId`可选 |
| POST | `/api/v1/alipay/create_pay_order` | 创建商城订单和支付宝支付单 | 商城JWT |
| POST | `/api/v1/alipay/query_user_order_list` | 游标分页查询当前用户订单 | 商城JWT |
| POST | `/api/v1/alipay/refund_order` | 申请退单/退款 | 商城JWT |
| POST | `/api/v1/alipay/pay_notify` | 支付宝异步支付回调 | 支付宝签名 |
| POST | `/api/v1/alipay/group_buy_notify` | 接收拼团成团通知 | `X-Group-Buy-Token` |
| GET | `/api/v1/login/weixin_qrcode_ticket` | 获取旧版微信二维码ticket | 已废弃、公开 |
| GET | `/api/v1/login/check_login` | 旧版扫码登录轮询 | 已废弃、公开 |
| GET | `/api/v1/login/weixin_qrcode_ticket_scene` | 创建场景绑定二维码ticket | 公开 |
| GET | `/api/v1/login/check_login_scene` | 场景绑定扫码登录轮询 | 公开 |
| GET | `/api/v1/weixin/portal/receive` | 微信公众号服务器验签 | 微信签名 |
| POST | `/api/v1/weixin/portal/receive` | 微信公众号扫码事件回调 | 微信签名 |

### 3.3 RAG客服服务（8095）

| 方法 | 路径 | 功能 | 鉴权 |
|---|---|---|---|
| POST | `/api/v1/admin/login` | 管理员登录 | 公开 |
| POST | `/api/v1/chat` | RAG客服问答 | 当前公开 |
| POST | `/api/v1/documents` | 上传文档并提交异步解析任务 | 管理员Token |
| GET | `/api/v1/documents/{documentId}` | 查询文档处理结果 | 管理员Token |
| DELETE | `/api/v1/documents/{documentId}` | 删除文档元数据和ES分片 | 管理员Token |

---

## 4. 拼团营销接口详情

源码入口：[`ekko-group-buy-market-trigger/http`](ekko-group-buy-market/ekko-group-buy-market-trigger/src/main/java/cn/ekko/trigger/http)

### 4.1 查询拼团营销配置

`POST /api/v1/gbm/index/query_ekko_group_buy_market_config`

请求JSON：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `userId` | string | 是 | 用户ID，也是限流键 |
| `source` | string | 是 | 业务来源 |
| `channel` | string | 是 | 业务渠道 |
| `goodsId` | string | 是 | 商品ID |

响应`data`：

| 字段 | 类型 | 说明 |
|---|---|---|
| `activityId` | long | 活动ID |
| `goods.goodsId` | string | 商品ID |
| `goods.originalPrice` | decimal | 原价 |
| `goods.deductionPrice` | decimal | 优惠金额 |
| `goods.payPrice` | decimal | 应付金额 |
| `teamList[]` | array | 当前用户队伍和推荐队伍 |
| `teamList[].userId` | string | 队伍展示用户 |
| `teamList[].teamId` | string | 队伍ID |
| `teamList[].activityId` | long | 活动ID |
| `teamList[].targetCount` | int | 目标人数 |
| `teamList[].completeCount` | int | 已完成人数 |
| `teamList[].lockCount` | int | 已锁人数 |
| `teamList[].validStartTime` | datetime | 开始时间 |
| `teamList[].validEndTime` | datetime | 截止时间 |
| `teamList[].validTimeCountdown` | string | 剩余时间展示值 |
| `teamList[].outTradeNo` | string | 当前用户外部交易号 |
| `teamStatistic.allTeamCount` | int | 队伍总数 |
| `teamStatistic.allTeamCompleteCount` | int | 已成团队伍数 |
| `teamStatistic.allTeamUserCount` | int | 参与用户量统计 |

行为：优惠试算 → 查询进行中队伍 → 查询活动统计。接口按`userId`限流，fallback返回业务码`0006`。  
证据：[`MarketIndexController.java`](ekko-group-buy-market/ekko-group-buy-market-trigger/src/main/java/cn/ekko/trigger/http/MarketIndexController.java)

### 4.2 拼团锁单

`POST /api/v1/gbm/trade/lock_market_pay_order`

请求JSON：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `userId` | string | 是 | 用户ID |
| `teamId` | string | 否 | 加入已有队伍；空值表示创建/匹配新队伍 |
| `activityId` | long | 是 | 活动ID |
| `goodsId` | string | 是 | 商品ID |
| `source` | string | 是 | 来源 |
| `channel` | string | 是 | 渠道 |
| `outTradeNo` | string | 是 | 外部交易号；商城传自己的订单号 |
| `notifyConfigVO.notifyType` | string | 是 | `HTTP`或`MQ` |
| `notifyConfigVO.notifyMQ` | string | MQ时使用 | MQ routing key |
| `notifyConfigVO.notifyUrl` | string | HTTP时必填 | 回调地址，必须命中服务端白名单 |

成功响应`data`：`orderId`、`teamId`、`originalPrice`、`deductionPrice`、`payPrice`、`tradeOrderStatus`。

行为和幂等：

1. 先按`userId + outTradeNo`查询已有记录。
2. 已存在时直接返回原价格快照，不再次占名额。
3. 新订单校验队伍容量、人群资格和回调配置。
4. 写入队伍/订单明细并占用名额。

证据：[`MarketTradeController.java`](ekko-group-buy-market/ekko-group-buy-market-trigger/src/main/java/cn/ekko/trigger/http/MarketTradeController.java)、[`LockMarketPayOrderRequestDTO.java`](ekko-group-buy-market/ekko-group-buy-market-api/src/main/java/cn/ekko/api/dto/LockMarketPayOrderRequestDTO.java)

### 4.3 拼团支付结算

`POST /api/v1/gbm/trade/settlement_market_pay_order`

请求JSON：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `source` | string | 是 | 来源 |
| `channel` | string | 是 | 渠道 |
| `userId` | string | 是 | 用户ID |
| `outTradeNo` | string | 是 | 商城订单号/外部交易号 |
| `outTradeTime` | datetime | 是 | 支付完成时间 |

成功响应`data`：`userId`、`teamId`、`activityId`、`outTradeNo`。

行为：将成员明细从创建更新为完成；增加队伍完成数；达到目标时把队伍标记为完成并创建通知任务。条件更新与状态回查承担幂等控制。  
证据：[`MarketTradeController.java`](ekko-group-buy-market/ekko-group-buy-market-trigger/src/main/java/cn/ekko/trigger/http/MarketTradeController.java)

### 4.4 拼团营销退单

`POST /api/v1/gbm/trade/refund_market_pay_order`

请求JSON：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `userId` | string | 是 | 用户ID |
| `outTradeNo` | string | 是 | 商城订单号/外部交易号 |
| `source` | string | 是 | 来源 |
| `channel` | string | 是 | 渠道 |

成功响应`data`：`userId`、`orderId`、`teamId`、`code`、`info`。

退单类型：

| code | 业务场景 |
|---|---|
| `unpaid_unlock` | 未支付、未成团，释放名额 |
| `paid_unformed` | 已支付、未成团，释放名额并通知商城退款 |
| `paid_formed` | 已支付、已成团，更新队伍状态并通知商城退款 |

该接口只处理营销侧状态，不直接操作支付宝资金。  
证据：[`RefundMarketPayOrderRequestDTO.java`](ekko-group-buy-market/ekko-group-buy-market-api/src/main/java/cn/ekko/api/dto/RefundMarketPayOrderRequestDTO.java)、[`RefundTypeEnumVO.java`](ekko-group-buy-market/ekko-group-buy-market-domain/src/main/java/cn/ekko/domain/trade/model/valobj/RefundTypeEnumVO.java)

### 4.5 动态配置更新

`GET /api/v1/gbm/dcc/update_config?key={key}&value={value}`

| 参数 | 位置 | 必填 | 说明 |
|---|---|---|---|
| `key` | query | 是 | 动态属性名，如`downgradeSwitch`、`cutRange`、`rateLimiterSwitch` |
| `value` | query | 是 | 新值 |

行为：向Redis Topic发布`AttributeVO(key,value)`；返回`Response<Boolean>`，当前成功分支没有显式设置`data=true`。当前源码没有鉴权和key白名单，是生产部署前必须收口的管理接口。  
证据：[`DCCController.java`](ekko-group-buy-market/ekko-group-buy-market-trigger/src/main/java/cn/ekko/trigger/http/DCCController.java)

### 4.6 回调测试接口

`POST /api/v1/test/group_buy_notify`

请求：`{"teamId":"...","outTradeNoList":["..."]}`。当前只打印日志并固定返回裸字符串`success`，不改变订单状态。生产环境不应公开。  
证据：[`TestApiClientController.java`](ekko-group-buy-market/ekko-group-buy-market-trigger/src/main/java/cn/ekko/trigger/http/TestApiClientController.java)

### 4.7 拼团营销业务码

| code | 含义 |
|---|---|
| `0000` | 成功 |
| `0001` | 未知失败 |
| `0002` | 非法参数 |
| `0003` | 唯一索引冲突 |
| `0004` | 更新记录为0 |
| `0005` | HTTP调用异常 |
| `0006` | 接口限流 |
| `E0001` | 不存在折扣计算服务 |
| `E0002` | 无拼团营销配置 |
| `E0003` | 活动降级拦截 |
| `E0004` | 活动切量拦截 |
| `E0005` | 组队失败，更新记录为0 |
| `E0006` | 队伍锁单量已达目标 |
| `E0007` | 人群限制，不可参与 |
| `E0008` | 缓存名额不足 |
| `E0101` | 活动未生效 |
| `E0102` | 不在活动有效时间内 |
| `E0103` | 用户参与次数达上限 |
| `E0104` | 外部交易号不存在或已退单 |
| `E0105` | SC渠道黑名单拦截 |
| `E0106` | 交易时间超出拼团有效范围 |

---

## 5. 支付商城接口详情

源码入口：[`ekko-pay-mall-trigger/http`](ekko-pay-mall/ekko-pay-mall-trigger/src/main/java/cn/ekko/trigger/http)

### 5.1 商品列表

`GET /api/v1/products`

无参数。只返回`status = 1`的商品。

`data[]`字段：`productId`、`productName`、`productDesc`、`productModel`、`productSpecs`、`basePrice`、`imageUrl`。列表查询不会组装`groupBuyMarket`。  
证据：[`ProductController.java`](ekko-pay-mall/ekko-pay-mall-trigger/src/main/java/cn/ekko/trigger/http/ProductController.java)

### 5.2 商品详情

`GET /api/v1/products/{productId}?userId={userId}`

| 参数 | 位置 | 必填 | 说明 |
|---|---|---|---|
| `productId` | path | 是 | 商品业务ID |
| `userId` | query | 否 | 用于查询用户相关拼团资格/队伍，当前接口不校验JWT |

除商品字段外，`data.groupBuyMarket`可能包含：`activityId`、`originalPrice`、`deductionPrice`、`payPrice`、`teamList[]`、`teamStatistic`。没有有效拼团配置时该对象可为空。  
证据：[`QueryProductResponseDTO.java`](ekko-pay-mall/ekko-pay-mall-api/src/main/java/cn/ekko/api/dto/QueryProductResponseDTO.java)

### 5.3 创建支付订单

`POST /api/v1/alipay/create_pay_order`

Header：`Authorization: Bearer <mall-jwt>`。

请求JSON：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `userId` | string | 是 | 必须与JWT subject一致；JWT身份是权威值 |
| `productId` | string | 是 | 商品ID |
| `teamId` | string | 否 | 加入指定队伍 |
| `activityId` | long | `marketType=1`时是 | 拼团活动ID |
| `marketType` | int | 否 | `0`无营销，`1`拼团；空值按0处理 |

成功时`data`是支付宝支付表单/支付地址字符串。

主流程：校验JWT → 读取商城商品 → 创建本地`CREATE`订单 → 拼团订单调用营销锁单 → 保存营销价格快照 → 使用`payAmount`创建支付宝单 → 更新为`PAY_WAIT`。已有未支付订单会走幂等读取/补建支付单。  
证据：[`AliPayController.java`](ekko-pay-mall/ekko-pay-mall-trigger/src/main/java/cn/ekko/trigger/http/AliPayController.java)、[`CreatePayRequestDTO.java`](ekko-pay-mall/ekko-pay-mall-api/src/main/java/cn/ekko/api/dto/CreatePayRequestDTO.java)

### 5.4 查询用户订单列表

`POST /api/v1/alipay/query_user_order_list`

Header：`Authorization: Bearer <mall-jwt>`。

请求：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `userId` | string | 是 | 必须与JWT subject一致 |
| `lastId` | long | 否 | 下一页游标，必须大于0 |
| `pageSize` | int | 否 | 默认10，范围1到50 |

响应`data`：

| 字段 | 说明 |
|---|---|
| `orderList[]` | 本页订单 |
| `orderList[].id` | 表主键/游标 |
| `orderList[].userId` | 用户ID |
| `orderList[].productId/productName` | 商品快照 |
| `orderList[].orderId` | 商城订单号 |
| `orderList[].orderTime` | 下单时间 |
| `orderList[].totalAmount` | 总金额快照 |
| `orderList[].status` | 订单状态字符串 |
| `orderList[].payUrl` | 支付地址 |
| `orderList[].marketType` | 0无营销、1拼团 |
| `orderList[].marketDeductionAmount` | 优惠金额 |
| `orderList[].payAmount` | 实付金额 |
| `orderList[].payTime` | 支付时间 |
| `hasMore` | 是否还有下一页 |
| `lastId` | 下一页应提交的游标 |

### 5.5 申请退单

`POST /api/v1/alipay/refund_order`

Header：`Authorization: Bearer <mall-jwt>`。

请求：`userId`、`orderId`均必填，且`userId`必须与JWT一致。

成功响应：

| 字段 | 说明 |
|---|---|
| `userId` | 当前用户 |
| `orderId` | 商城订单号 |
| `status` | `CLOSE`或`WAIT_REFUND` |
| `refundRequired` | 是否需要支付宝退款 |
| `info` | 处理说明 |

行为：未支付订单先通知营销退单，成功后直接关闭；已支付订单先完成营销退单，再进入`WAIT_REFUND`，等待RabbitMQ消费者或补偿任务调用支付宝退款。  
证据：[`RefundOrderRequestDTO.java`](ekko-pay-mall/ekko-pay-mall-api/src/main/java/cn/ekko/api/dto/RefundOrderRequestDTO.java)

### 5.6 支付宝支付回调

`POST /api/v1/alipay/pay_notify`

Content-Type为支付宝表单。代码实际使用：`sign`、`trade_status`、`out_trade_no`、`gmt_payment`、`trade_no`以及验签所需的完整参数集合。

- 验签失败：返回`failure`。
- 非`TRADE_SUCCESS`：不执行支付副作用，返回`success`。
- 成功：条件更新`PAY_WAIT → PAY_SUCCESS`；拼团订单继续调用营销结算；最终返回`success`或`failure`。
- `gmt_payment`格式严格为`yyyy-MM-dd HH:mm:ss`。

### 5.7 拼团成团回调

`POST /api/v1/alipay/group_buy_notify`

Header：`X-Group-Buy-Token`必须与商城配置的共享Token一致。

请求：

```json
{
  "teamId": "TEAM-001",
  "outTradeNoList": ["ORDER-001", "ORDER-002"]
}
```

行为：校验队伍和订单集合，把符合条件的拼团支付订单从`PAY_SUCCESS`更新为`MARKET`；返回裸字符串`success`或`error`。  
证据：[`AliPayController.java`](ekko-pay-mall/ekko-pay-mall-trigger/src/main/java/cn/ekko/trigger/http/AliPayController.java)

### 5.8 旧版微信二维码登录（已废弃）

- `GET /api/v1/login/weixin_qrcode_ticket`：返回微信二维码ticket。
- `GET /api/v1/login/check_login?ticket=...`：轮询扫码状态，成功返回1小时JWT。

新接口生成的ticket不能通过旧轮询接口绕过`sceneStr`校验。新前端不应继续接入这两个接口。

### 5.9 场景绑定微信二维码登录

创建二维码：

`GET /api/v1/login/weixin_qrcode_ticket_scene?sceneStr={sceneStr}`

- `sceneStr`会转大写。
- 只允许`[A-Z0-9_-]{1,64}`。
- 服务在Guava Cache中保存`sceneStr ↔ ticket`双向绑定，默认5分钟过期。

轮询登录：

`GET /api/v1/login/check_login_scene?ticket={ticket}&sceneStr={sceneStr}`

- ticket与sceneStr必须双向匹配。
- 微信回调已写入扫码openid后，返回1小时JWT；未扫码返回业务码`0003`。

证据：[`LoginController.java`](ekko-pay-mall/ekko-pay-mall-trigger/src/main/java/cn/ekko/trigger/http/LoginController.java)、[`WeixinLoginService.java`](ekko-pay-mall/ekko-pay-mall-domain/src/main/java/cn/ekko/domain/auth/service/WeixinLoginService.java)

### 5.10 微信公众号服务器验签

`GET /api/v1/weixin/portal/receive`

Query参数：`signature`、`timestamp`、`nonce`、`echostr`，均必填。签名正确时原样返回`echostr`，失败返回空响应。

### 5.11 微信公众号事件回调

`POST /api/v1/weixin/portal/receive`

Query必填：`signature`、`timestamp`、`nonce`、`openid`；`encrypt_type`、`msg_signature`可选。Body为微信公众号XML。

只有`MsgType=event`且`Event=SCAN/subscribe`并携带`Ticket`时，才保存`ticket → openid`扫码结果；响应微信公众号XML文本。  
证据：[`WeixinPortalController.java`](ekko-pay-mall/ekko-pay-mall-trigger/src/main/java/cn/ekko/trigger/http/WeixinPortalController.java)

### 5.12 支付商城业务码

| code | 含义 |
|---|---|
| `0000` | 成功 |
| `0001` | 未知失败 |
| `0002` | 非法参数 |
| `1001` | 拼团HTTP调用失败 |
| `1002` | 拼团返回空响应 |
| `1003` | 拼团业务失败 |
| `1004` | 拼团返回数据不完整 |
| `1005` | 支付金额无效 |
| `1006` | 未登录或JWT失效 |
| `1007` | 订单不存在或不属于当前用户 |
| `1008` | 当前订单状态不允许退单 |
| `1009` | 商品不存在或已下架 |

登录接口另使用`0003=未登录/尚未扫码`。

---

## 6. RAG客服接口详情

### 6.1 管理员登录

`POST /api/v1/admin/login`

请求：

```json
{
  "username": "admin",
  "password": "******"
}
```

成功响应：

```json
{
  "username": "admin",
  "token": "..."
}
```

失败返回HTTP 401。当前实现启动时生成一个进程内随机Token，所有成功登录共享该Token，服务重启后旧Token全部失效；当前没有过期时间、登出和多会话表。  
证据：[`AdminAuthController.java`](ekko-group-chat/src/main/java/cn/ekko/groupchat/auth/AdminAuthController.java)、[`AdminAuthService.java`](ekko-group-chat/src/main/java/cn/ekko/groupchat/auth/AdminAuthService.java)

### 6.2 RAG问答

`POST /api/v1/chat`

请求：

```json
{
  "question": "TL-7DR5130有几个2.5G网口？"
}
```

成功响应：

```json
{
  "answer": "根据资料，该型号有1个2.5G网口。[资料1]",
  "sources": [
    {
      "documentId": 1,
      "title": "TL-7DR5130产品详介",
      "chunkIndex": 0,
      "sourceObjectKey": "knowledge/parsed/1/xxx.md",
      "text": "...",
      "score": 0.86
    }
  ]
}
```

- `question`不能为空。
- 默认向量检索最多5段，最低分0.65。
- 没有命中时返回“现有知识库无法确认，请补充相关产品资料。”和空`sources`。
- 当前接口公开，未设置用户JWT、限流或对话历史。

证据：[`ChatController.java`](ekko-group-chat/src/main/java/cn/ekko/groupchat/chat/ChatController.java)、[`RagChatService.java`](ekko-group-chat/src/main/java/cn/ekko/groupchat/chat/RagChatService.java)

### 6.3 上传知识文档

`POST /api/v1/documents`

Header：`Authorization: Bearer <admin-token>`。  
Content-Type：`multipart/form-data`。

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `file` | file | 是 | PDF、PNG、JPG或JPEG |
| `title` | string | 是 | 文档标题 |
| `productModel` | string | 否 | 产品型号元数据 |

返回HTTP 201及`KnowledgeDocument`：`id`、`title`、`fileName`、`contentType`、`fileSize`、`sha256`、`originalObjectKey`、`parsedObjectKey`、`productModel`、`status`、`mineruTaskId`、`mineruSubmittedAt`、`chunkCount`、`failureReason`、`createdAt`、`updatedAt`。

当前是异步链路：MySQL建记录 → 原文件上传私有OSS → 生成短期签名下载URL → 提交MinerU官方`vlm`任务 → 保存`taskId`并以`PARSING`立即返回。后台默认每10秒查询任务；完成后下载`full_zip_url`、提取`full.md`、保存Markdown、分片、Embedding、写Elasticsearch并推进为`PUBLISHED`。提交任务失败时接口返回HTTP 502并标记`FAILED`；后台临时查询失败会保留`PARSING`等待下一轮。  
证据：[`DocumentController.java`](ekko-group-chat/src/main/java/cn/ekko/groupchat/document/controller/DocumentController.java)、[`DocumentService.java`](ekko-group-chat/src/main/java/cn/ekko/groupchat/document/service/DocumentService.java)、[`MineruPollingJob.java`](ekko-group-chat/src/main/java/cn/ekko/groupchat/document/job/MineruPollingJob.java)、[`MineruTaskProcessor.java`](ekko-group-chat/src/main/java/cn/ekko/groupchat/document/service/MineruTaskProcessor.java)

### 6.4 查询文档

`GET /api/v1/documents/{documentId}`

Header：`Authorization: Bearer <admin-token>`。返回完整`KnowledgeDocument`；不存在返回HTTP 404。

### 6.5 删除文档

`DELETE /api/v1/documents/{documentId}`

Header：`Authorization: Bearer <admin-token>`。成功返回HTTP 204。

删除顺序：先按`documentId`删除ES分片，再删除MySQL元数据；OSS原文件和Markdown保留。ES删除失败时MySQL记录保留，方便重试。

### 6.6 运维端点

- `GET /actuator/health`
- `GET /actuator/info`

它们由Spring Boot Actuator生成，不属于Controller源码；暴露范围来自客服服务配置。

---

## 7. 服务间与第三方出站接口

### 7.1 支付商城 → 拼团营销

商城通过Retrofit调用以下4个接口：

| 方法 | 路径 | 调用时机 |
|---|---|---|
| POST | `/api/v1/gbm/index/query_ekko_group_buy_market_config` | 商品详情查询拼团价格和队伍 |
| POST | `/api/v1/gbm/trade/lock_market_pay_order` | 创建拼团支付单前锁名额和价格 |
| POST | `/api/v1/gbm/trade/settlement_market_pay_order` | 支付事实落库后结算成员 |
| POST | `/api/v1/gbm/trade/refund_market_pay_order` | 商城退单时先处理营销状态 |

证据：[`IGroupBuyMarketService.java`](ekko-pay-mall/ekko-pay-mall-infrastructure/src/main/java/cn/ekko/infrastructure/gateway/IGroupBuyMarketService.java)

### 7.2 拼团营销 → 支付商城

- 成团时依据锁单保存的通知配置走HTTP或MQ。
- HTTP回调目标通常为商城`POST /api/v1/alipay/group_buy_notify`。
- HTTP请求携带`X-Group-Buy-Token`并要求响应正文为`success`。
- MQ模式按`notifyMQ` routing key发布`parameter_json`。

证据：[`GroupBuyNotifyService.java`](ekko-group-buy-market/ekko-group-buy-market-infrastructure/src/main/java/cn/ekko/infrastructure/gateway/GroupBuyNotifyService.java)、[`TradePort.java`](ekko-group-buy-market/ekko-group-buy-market-infrastructure/src/main/java/cn/ekko/infrastructure/adapter/port/TradePort.java)

### 7.3 支付商城 → 微信公众号API

| 方法 | 微信路径 | 用途 |
|---|---|---|
| GET | `cgi-bin/token` | 获取access token |
| POST | `cgi-bin/qrcode/create` | 创建参数二维码ticket |
| POST | `cgi-bin/message/template/send` | 发送登录成功模板消息 |

证据：[`IWeixinApiService.java`](ekko-pay-mall/ekko-pay-mall-infrastructure/src/main/java/cn/ekko/infrastructure/gateway/IWeixinApiService.java)

### 7.4 支付商城 → 支付宝SDK

| SDK请求 | 用途 |
|---|---|
| `AlipayTradePagePayRequest` | 创建网页支付表单 |
| `AlipayTradeQueryRequest` | 补查漏掉的支付回调 |
| `AlipayTradeRefundRequest` | 发起退款，稳定退款号为`REFUND_{orderId}` |
| `AlipayTradeFastpayRefundQueryRequest` | 退款前后查询退款状态以保证幂等 |

只有退款响应`fund_change=Y`或查询明确返回`REFUND_SUCCESS`时，才将本地`WAIT_REFUND`更新为`CLOSE`。

### 7.5 RAG客服外部依赖

| 外部接口 | 用途 |
|---|---|
| 阿里云OSS SDK | 保存原始PDF/图片和解析后的Markdown，并为私有原文件生成短期签名GET URL |
| MinerU `POST /api/v4/extract/task` | 使用签名源文件URL创建官方`vlm`异步解析任务 |
| MinerU `GET /api/v4/extract/task/{taskId}` | 查询`pending/running/converting/done/failed`任务状态 |
| MinerU结果`full_zip_url` | 不携带MinerU Authorization下载结果ZIP，并从中安全提取`full.md` |
| OpenAI兼容Chat API | 调用配置的Qwen聊天模型 |
| OpenAI兼容Embedding API | 调用`text-embedding-v4`等向量模型 |
| Elasticsearch API | 写入、检索、按文档删除向量分片 |

---

## 8. RabbitMQ消息接口

### 8.1 拼团退款消息

消息JSON字段：

| 字段 | 必填 | 说明 |
|---|---|---|
| `type` | 是 | `unpaid_unlock`、`paid_unformed`、`paid_formed` |
| `userId` | 是 | 用户ID |
| `teamId` | 是 | 队伍ID |
| `activityId` | 是 | 活动ID |
| `orderId` | 是 | 营销侧预购订单ID |
| `outTradeNo` | 是 | 商城订单号 |

消费者：

1. 拼团营销消费者恢复队伍Redis锁单库存；异常抛出触发MQ重试。
2. 支付商城使用独立队列消费同一类消息：未支付订单确认关闭；已支付订单调用支付宝退款；异常抛出并最终进入DLQ。

证据：[`TeamRefundSuccess.java`](ekko-group-buy-market/ekko-group-buy-market-domain/src/main/java/cn/ekko/domain/trade/model/valobj/TeamRefundSuccess.java)、[`商城RefundSuccessTopicListener.java`](ekko-pay-mall/ekko-pay-mall-trigger/src/main/java/cn/ekko/trigger/listener/RefundSuccessTopicListener.java)、[`营销RefundSuccessTopicListener.java`](ekko-group-buy-market/ekko-group-buy-market-trigger/src/main/java/cn/ekko/trigger/listener/RefundSuccessTopicListener.java)

### 8.2 成团成功消息

拼团营销的`TeamSuccessTopicListener`监听配置的成团成功队列，当前实现只记录消息日志，没有后续业务状态变化。  
证据：[`TeamSuccessTopicListener.java`](ekko-group-buy-market/ekko-group-buy-market-trigger/src/main/java/cn/ekko/trigger/listener/TeamSuccessTopicListener.java)

---

## 9. 定时任务

| 服务 | 任务 | 实际Cron | 作用 |
|---|---|---|---|
| 拼团营销 | `GroupBuyNotifyJob` | 默认每10秒 | 抢占`notify_task`并发送HTTP/MQ通知；Redis分布式锁防多实例重复扫描 |
| 拼团营销 | `TimeoutRefundJob` | 每1分钟 | 扫描已过期且未支付的拼团明细并退单；源码注释写“每5分钟”但与注解不一致，以注解为准 |
| 支付商城 | `MarketPaySettlementJob` | 每10秒 | 补偿`PAY_SUCCESS`拼团订单的远程结算 |
| 支付商城 | `NoPayNotifyOrderJob` | 每3秒 | 查询超过1分钟仍`PAY_WAIT`的支付宝交易并补记支付成功 |
| 支付商城 | `TimeoutCloseOrderJob` | 每10分钟 | 关闭超过30分钟仍`PAY_WAIT`的订单 |
| 支付商城 | `RefundCompensationJob` | 默认每30秒 | 扫描超时`WAIT_REFUND`订单，查询/重试支付宝退款 |

`@Scheduled`存在只证明代码会注册任务；是否实际运行仍取决于服务实例、`@EnableScheduling`、配置、数据库、Redis和外部依赖。

---

## 10. 当前安全与联调边界

1. 拼团营销和支付商城Controller仍使用`@CrossOrigin("*")`，生产环境应限制来源。
2. 拼团锁单、结算、退单接口目前没有明确的服务间身份认证；不能仅依赖“只有商城知道地址”。
3. DCC更新和测试回调接口当前未鉴权，不应直接暴露公网。
4. 商城用户级接口以JWT subject为身份，body中的`userId`只做一致性校验。
5. 拼团成团HTTP回调使用共享Token；生产部署必须通过环境变量/Secret注入并轮换。
6. 客服文档接口需要管理员Token，但问答接口当前公开；管理员Token是单进程共享内存值，不是完整会话系统。
7. 当前源码与单元测试不能证明微信、支付宝、RabbitMQ、Redis、MySQL、OSS、MinerU、模型和Elasticsearch端到端成功，联调需保留真实请求响应、日志和数据状态证据。

## 11. 前端实际使用的接口

当前`ekko-group-buy-web`已调用：

- 商品列表/详情
- 创建支付单、订单列表、退单
- 场景绑定微信二维码及登录轮询
- RAG问答
- 客服管理员登录
- 文档上传、查询和删除

证据：[`mall.js`](ekko-group-buy-web/src/api/mall.js)、[`knowledge.js`](ekko-group-buy-web/src/api/knowledge.js)
