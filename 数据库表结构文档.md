# 当前项目数据库表结构文档

> 更新时间：2026-08-02  
> 扫描范围：`ekko-group-buy-market`、`ekko-pay-mall`、`ekko-group-chat`  
> 本文只整理当前工作区能够由源码证明的结构，没有连接或修改真实数据库。

## 1. 证据口径

- `ekko-group-chat` 提供了当前建表文件，字段类型、主键和索引以 [`schema.sql`](ekko-group-chat/src/main/resources/db/schema.sql) 为准。
- `ekko-group-buy-market` 与 `ekko-pay-mall` 当前没有 DDL 文件，字段来自 MyBatis Mapper、PO 和实际 SQL。此类表只能确认“代码读写了哪些字段”，不能仅凭源码确认 VARCHAR 长度、是否允许 NULL、默认值、主键、唯一索引和外键。
- 上线或变更数据库前，必须对真实库执行 `SHOW CREATE TABLE <table>;` 和 `SHOW INDEX FROM <table>;` 复核。本文不能替代真实库结构。
- Java `String/Long/Integer/BigDecimal/Date` 是映射类型，不等同于已经确认的 MySQL `VARCHAR/BIGINT/INT/DECIMAL/DATETIME` 定义。

## 2. 总览

| 服务 | 表名 | 主要用途 | 结构证据 |
|---|---|---|---|
| 拼团营销 | `crowd_tags` | 人群标签及命中人数 | PO + Mapper |
| 拼团营销 | `crowd_tags_detail` | 标签与用户关系 | PO + Mapper |
| 拼团营销 | `crowd_tags_job` | 标签批次统计任务 | PO + Mapper |
| 拼团营销 | `group_buy_activity` | 拼团活动配置 | PO + Mapper/SQL |
| 拼团营销 | `group_buy_discount` | 优惠规则配置 | PO + Mapper |
| 拼团营销 | `sc_sku_activity` | 来源、渠道、商品与活动映射 | PO + Mapper |
| 拼团营销 | `sku` | 营销侧商品及原价 | PO + Mapper |
| 拼团营销 | `group_buy_order` | 拼团队伍及人数进度 | PO + Mapper |
| 拼团营销 | `group_buy_order_list` | 用户参与拼团的订单明细 | PO + Mapper |
| 拼团营销 | `notify_task` | 成团/退款可靠通知任务 | PO + Mapper |
| 支付商城 | `product` | 商城商品主数据 | PO + Mapper |
| 支付商城 | `pay_order` | 商城订单、支付与退款状态 | PO + Mapper |
| RAG 客服 | `knowledge_document` | 知识文档元数据和入库状态 | 当前 DDL |
| RAG 客服 | `admin_user` | 知识库管理账号 | 当前 DDL |

共识别 **14 张关系表**：其中 2 张有当前 DDL，12 张只有源码映射证据。

---

## 3. 拼团营销库

源码目录：[`ekko-group-buy-market`](ekko-group-buy-market)

### 3.1 `crowd_tags`

用途：保存人群标签定义以及标签命中人数。`statistics` 通过增量 SQL 更新。

| 字段 | Java类型 | 含义 |
|---|---|---|
| `id` | `Long` | 记录ID，代码映射为标识字段 |
| `tag_id` | `String` | 标签业务ID |
| `tag_name` | `String` | 标签名称 |
| `tag_desc` | `String` | 标签说明 |
| `statistics` | `Integer` | 标签命中用户数量 |
| `create_time` | `Date` | 创建时间 |
| `update_time` | `Date` | 更新时间 |

主要操作：按 `tag_id` 执行 `statistics = statistics + ?`。  
证据：[`CrowdTags.java`](ekko-group-buy-market/ekko-group-buy-market-infrastructure/src/main/java/cn/ekko/infrastructure/dao/po/CrowdTags.java)、[`crowd_tags_mapper.xml`](ekko-group-buy-market/ekko-group-buy-market-app/src/main/resources/mybatis/mapper/crowd_tags_mapper.xml)

### 3.2 `crowd_tags_detail`

用途：记录某个用户属于哪个人群标签。

| 字段 | Java类型 | 含义 |
|---|---|---|
| `id` | `Long` | 记录ID |
| `tag_id` | `String` | 标签业务ID |
| `user_id` | `String` | 用户ID |
| `create_time` | `Date` | 创建时间 |
| `update_time` | `Date` | 更新时间 |

主要操作：插入 `tag_id + user_id` 关系。实际库应复核二者是否有防重复唯一约束。  
证据：[`CrowdTagsDetail.java`](ekko-group-buy-market/ekko-group-buy-market-infrastructure/src/main/java/cn/ekko/infrastructure/dao/po/CrowdTagsDetail.java)、[`crowd_tags_detail_mapper.xml`](ekko-group-buy-market/ekko-group-buy-market-app/src/main/resources/mybatis/mapper/crowd_tags_detail_mapper.xml)

### 3.3 `crowd_tags_job`

用途：保存标签批次任务及统计时间范围。

| 字段 | Java类型 | 含义 |
|---|---|---|
| `id` | `Long` | 记录ID |
| `tag_id` | `String` | 标签业务ID |
| `batch_id` | `String` | 批次ID |
| `tag_type` | `Integer` | 标签类型 |
| `tag_rule` | `String` | 标签计算规则 |
| `stat_start_time` | `Date` | 统计开始时间 |
| `stat_end_time` | `Date` | 统计结束时间 |
| `status` | `Integer` | 任务状态，当前源码未提供完整枚举 |
| `create_time` | `Date` | 创建时间 |
| `update_time` | `Date` | 更新时间 |

主要查询键：`tag_id + batch_id`。  
证据：[`CrowdTagsJob.java`](ekko-group-buy-market/ekko-group-buy-market-infrastructure/src/main/java/cn/ekko/infrastructure/dao/po/CrowdTagsJob.java)、[`crowd_tags_job_mapper.xml`](ekko-group-buy-market/ekko-group-buy-market-app/src/main/resources/mybatis/mapper/crowd_tags_job_mapper.xml)

### 3.4 `group_buy_activity`

用途：保存拼团活动、成团目标、活动时间、人群限制及折扣关联。

| 字段 | Java类型/来源 | 含义 |
|---|---|---|
| `id` | `Long` | 记录ID |
| `activity_id` | `Long` | 活动业务ID |
| `activity_name` | `String` | 活动名称 |
| `discount_id` | `String` | 关联优惠ID |
| `group_type` | `Integer` | 拼团类型，当前源码未提供完整枚举说明 |
| `take_limit_count` | `Integer` | 单用户参与次数限制 |
| `target` | `Integer` | 成团目标人数 |
| `valid_time` | `Integer` | 队伍有效时长 |
| `status` | `Integer` | `0`创建、`1`生效、`2`过期、`3`废弃 |
| `start_time` | `Date` | 活动开始时间 |
| `end_time` | `Date` | 活动结束时间 |
| `tag_id` | `String` | 限定人群标签ID |
| `tag_scope` | `String` | 标签适用范围 |
| `source` | SQL引用 | 来源；查询条件使用，但当前 PO/ResultMap 未声明该字段 |
| `channel` | SQL引用 | 渠道；查询条件使用，但当前 PO/ResultMap 未声明该字段 |
| `create_time` | `Date` | 创建时间 |
| `update_time` | `Date` | 更新时间 |

主要查询键：`activity_id`，以及 `source + channel`；有效活动要求 `status = 1`。  
证据：[`GroupBuyActivity.java`](ekko-group-buy-market/ekko-group-buy-market-infrastructure/src/main/java/cn/ekko/infrastructure/dao/po/GroupBuyActivity.java)、[`group_buy_activity_mapper.xml`](ekko-group-buy-market/ekko-group-buy-market-app/src/main/resources/mybatis/mapper/group_buy_activity_mapper.xml)

### 3.5 `group_buy_discount`

用途：保存拼团优惠规则、表达式及适用人群。

| 字段 | Java类型 | 含义 |
|---|---|---|
| `id` | `Long` | 记录ID |
| `discount_id` | `String` | 优惠业务ID |
| `discount_name` | `String` | 优惠名称 |
| `discount_desc` | `String` | 优惠描述 |
| `discount_type` | `Integer` | `0`基础优惠、`1`人群标签优惠 |
| `market_plan` | `String` | 营销计划/计算方案 |
| `market_expr` | `String` | 优惠计算表达式 |
| `tag_id` | `String` | 关联标签ID |
| `create_time` | `Date` | 创建时间 |
| `update_time` | `Date` | 更新时间 |

主要查询键：`discount_id`。  
证据：[`GroupBuyDiscount.java`](ekko-group-buy-market/ekko-group-buy-market-infrastructure/src/main/java/cn/ekko/infrastructure/dao/po/GroupBuyDiscount.java)、[`group_buy_discount_mapper.xml`](ekko-group-buy-market/ekko-group-buy-market-app/src/main/resources/mybatis/mapper/group_buy_discount_mapper.xml)

### 3.6 `sc_sku_activity`

用途：把来源、渠道、商品映射到拼团活动。

| 字段 | Java类型 | 含义 |
|---|---|---|
| `id` | `Long` | 记录ID |
| `source` | `String` | 业务来源 |
| `channel` | `String` | 业务渠道 |
| `activity_id` | `Long` | 活动业务ID |
| `goods_id` | `String` | 商品业务ID |
| `create_time` | `Date` | 创建时间 |
| `update_time` | `Date` | 更新时间 |

主要查询键：`goods_id + source + channel`。  
证据：[`SCSkuActivity.java`](ekko-group-buy-market/ekko-group-buy-market-infrastructure/src/main/java/cn/ekko/infrastructure/dao/po/SCSkuActivity.java)、[`sc_sku_activity_mapper.xml`](ekko-group-buy-market/ekko-group-buy-market-app/src/main/resources/mybatis/mapper/sc_sku_activity_mapper.xml)

### 3.7 `sku`

用途：保存营销侧商品基础信息和原价，用于优惠试算。

| 字段 | Java类型 | 含义 |
|---|---|---|
| `id` | `Long` | 记录ID |
| `source` | `String` | 业务来源 |
| `channel` | `String` | 业务渠道 |
| `goods_id` | `String` | 商品业务ID |
| `goods_name` | `String` | 商品名称 |
| `original_price` | `BigDecimal` | 原价 |
| `create_time` | `Date` | 创建时间 |
| `update_time` | `Date` | 更新时间 |

主要查询键：`goods_id`。  
证据：[`Sku.java`](ekko-group-buy-market/ekko-group-buy-market-infrastructure/src/main/java/cn/ekko/infrastructure/dao/po/Sku.java)、[`sku_mapper.xml`](ekko-group-buy-market/ekko-group-buy-market-app/src/main/resources/mybatis/mapper/sku_mapper.xml)

### 3.8 `group_buy_order`

用途：一行代表一支拼团队伍，记录目标人数、锁定人数、已支付完成人数和队伍状态。

| 字段 | Java类型 | 含义 |
|---|---|---|
| `id` | `Long` | 记录ID |
| `team_id` | `String` | 拼团队伍ID |
| `activity_id` | `Long` | 活动业务ID |
| `source` | `String` | 来源 |
| `channel` | `String` | 渠道 |
| `original_price` | `BigDecimal` | 原价快照 |
| `deduction_price` | `BigDecimal` | 优惠金额快照 |
| `pay_price` | `BigDecimal` | 应付金额快照 |
| `target_count` | `Integer` | 成团目标人数 |
| `complete_count` | `Integer` | 已完成支付/结算人数 |
| `lock_count` | `Integer` | 已锁定名额数 |
| `status` | `Integer` | `0`拼单中、`1`完成、`2`失败、`3`完成但含退单 |
| `valid_start_time` | `Date` | 队伍有效期开始 |
| `valid_end_time` | `Date` | 队伍有效期结束 |
| `notify_type` | `String` | 成团通知方式：`HTTP`或`MQ` |
| `notify_url` | `String` | HTTP回调地址 |
| `create_time` | `Date` | 创建时间 |
| `update_time` | `Date` | 更新时间 |

并发更新通过条件 SQL 控制，例如 `lock_count < target_count`、`complete_count < lock_count`、`status = 0`。主要查询和更新键为 `team_id`。  
证据：[`GroupBuyOrder.java`](ekko-group-buy-market/ekko-group-buy-market-infrastructure/src/main/java/cn/ekko/infrastructure/dao/po/GroupBuyOrder.java)、[`group_buy_order_mapper.xml`](ekko-group-buy-market/ekko-group-buy-market-app/src/main/resources/mybatis/mapper/group_buy_order_mapper.xml)

### 3.9 `group_buy_order_list`

用途：保存每个用户参加拼团的交易明细和价格快照。

| 字段 | Java类型 | 含义 |
|---|---|---|
| `id` | `Long` | 记录ID |
| `user_id` | `String` | 用户ID |
| `team_id` | `String` | 队伍ID |
| `order_id` | `String` | 营销侧预购订单ID |
| `activity_id` | `Long` | 活动ID |
| `start_time` | `Date` | 订单/队伍开始时间 |
| `end_time` | `Date` | 有效截止时间 |
| `goods_id` | `String` | 商品ID |
| `source` | `String` | 来源 |
| `channel` | `String` | 渠道 |
| `original_price` | `BigDecimal` | 原价快照 |
| `deduction_price` | `BigDecimal` | 优惠金额快照 |
| `pay_price` | `BigDecimal` | 应付金额快照 |
| `status` | `Integer` | `0`初始创建、`1`消费完成、`2`用户退单 |
| `out_trade_no` | `String` | 外部交易号；当前商城对接中等于商城 `pay_order.order_id` |
| `out_trade_time` | `Date` | 外部支付完成时间 |
| `biz_id` | `String` | 业务幂等/关联ID |
| `create_time` | `Date` | 创建时间 |
| `update_time` | `Date` | 更新时间 |

主要查询键：`user_id + out_trade_no`、`activity_id + user_id`、`team_id`、`order_id`。源码通过唯一索引冲突分支处理重复锁单，但当前仓库没有 DDL，必须在真实库复核 `out_trade_no` 等唯一约束。  
证据：[`GroupBuyOrderList.java`](ekko-group-buy-market/ekko-group-buy-market-infrastructure/src/main/java/cn/ekko/infrastructure/dao/po/GroupBuyOrderList.java)、[`group_buy_order_list_mapper.xml`](ekko-group-buy-market/ekko-group-buy-market-app/src/main/resources/mybatis/mapper/group_buy_order_list_mapper.xml)

### 3.10 `notify_task`

用途：可靠记录成团回调和退款消息，供定时任务抢占、发送、重试及审计。

| 字段 | Java类型 | 含义 |
|---|---|---|
| `id` | `Long` | 记录ID |
| `activity_id` | `Long` | 活动ID |
| `team_id` | `String` | 队伍ID |
| `notify_category` | `String` | 通知业务类别 |
| `notify_type` | `String` | `HTTP`或`MQ` |
| `notify_mq` | `String` | MQ routing key |
| `notify_url` | `String` | HTTP回调地址 |
| `notify_count` | `Integer` | 已执行/重试次数 |
| `notify_status` | `Integer` | SQL约定：`0`待执行、`1`成功、`2`待重试、`3`终止失败、`4`处理中 |
| `parameter_json` | `String` | 发送的JSON内容 |
| `uuid` | `String` | 通知任务幂等标识 |
| `last_error` | `String` | 最近失败原因 |
| `create_time` | `Date` | 创建时间 |
| `update_time` | `Date` | 更新时间/抢占时间 |

任务通过 `team_id + uuid + notify_status` 条件抢占；处理中超过10分钟可重新抢占。真实库应复核 `uuid` 的唯一性和任务扫描索引。  
证据：[`NotifyTask.java`](ekko-group-buy-market/ekko-group-buy-market-infrastructure/src/main/java/cn/ekko/infrastructure/dao/po/NotifyTask.java)、[`nofify_task_mapper.xml`](ekko-group-buy-market/ekko-group-buy-market-app/src/main/resources/mybatis/mapper/nofify_task_mapper.xml)

---

## 4. 支付商城库

源码目录：[`ekko-pay-mall`](ekko-pay-mall)

### 4.1 `product`

用途：保存商城商品展示信息和基础售价。

| 字段 | Java类型 | 含义 |
|---|---|---|
| `id` | `Long` | 记录ID |
| `product_id` | `String` | 商品业务ID |
| `product_name` | `String` | 商品名称 |
| `product_desc` | `String` | 商品描述 |
| `product_model` | `String` | 产品型号，如路由器型号 |
| `product_specs` | `String` | 商品规格说明 |
| `base_price` | `BigDecimal` | 基础售价 |
| `image_url` | `String` | 商品图片地址 |
| `status` | `Integer` | 上架状态；当前查询只返回 `status = 1` |
| `create_time` | `Date` | 创建时间 |
| `update_time` | `Date` | 更新时间 |

主要查询键：`product_id`；列表按 `id` 升序。  
证据：[`Product.java`](ekko-pay-mall/ekko-pay-mall-infrastructure/src/main/java/cn/ekko/infrastructure/dao/po/Product.java)、[`product_mapper.xml`](ekko-pay-mall/ekko-pay-mall-app/src/main/resources/mybatis/mapper/product_mapper.xml)

### 4.2 `pay_order`

用途：商城订单事实表，保存商品快照、营销快照、支付状态和退款状态。

| 字段 | Java类型 | 含义 |
|---|---|---|
| `id` | `Long` | 记录ID，也是订单列表游标 |
| `user_id` | `String` | 下单用户ID |
| `product_id` | `String` | 商品业务ID |
| `product_name` | `String` | 下单时商品名称快照 |
| `order_id` | `String` | 商城订单号，同时作为支付宝 `out_trade_no` 和营销侧 `out_trade_no` |
| `order_time` | `Date` | 下单时间 |
| `total_amount` | `BigDecimal` | 原始/订单总金额快照 |
| `status` | `String` | 订单状态，见下表 |
| `pay_url` | `String` | 支付地址 |
| `pay_time` | `Date` | 支付成功时间 |
| `market_type` | `Integer` | `0`无营销、`1`拼团营销 |
| `activity_id` | `Long` | 拼团活动ID |
| `team_id` | `String` | 拼团队伍ID |
| `market_deduction_amount` | `BigDecimal` | 营销优惠金额快照 |
| `pay_amount` | `BigDecimal` | 实际应付/退款金额快照 |
| `create_time` | `Date` | 创建时间 |
| `update_time` | `Date` | 更新时间，也是退款补偿扫描依据 |

订单状态：

| 状态 | 含义 |
|---|---|
| `CREATE` | 本地订单已创建，尚未建立支付单 |
| `PAY_WAIT` | 已建立支付单，等待支付 |
| `PAY_SUCCESS` | 支付成功 |
| `MARKET` | 拼团已成团，等待履约 |
| `DEAL_DONE` | 交易履约完成 |
| `WAIT_REFUND` | 已申请营销退单，等待支付宝退款确认 |
| `CLOSE` | 订单关闭/退款完成 |

关键条件更新：`CREATE → PAY_WAIT`、`PAY_WAIT → PAY_SUCCESS`、`PAY_SUCCESS → MARKET`、`MARKET → DEAL_DONE`、已支付状态 → `WAIT_REFUND`、`WAIT_REFUND → CLOSE`。订单列表使用 `id < lastId ORDER BY id DESC LIMIT ?` 游标分页。  
证据：[`PayOrder.java`](ekko-pay-mall/ekko-pay-mall-infrastructure/src/main/java/cn/ekko/infrastructure/dao/po/PayOrder.java)、[`pay_order_mapper.xml`](ekko-pay-mall/ekko-pay-mall-app/src/main/resources/mybatis/mapper/pay_order_mapper.xml)、[`OrderStatusVO.java`](ekko-pay-mall/ekko-pay-mall-domain/src/main/java/cn/ekko/domain/order/model/valobj/OrderStatusVO.java)

---

## 5. RAG 客服库

源码目录：[`ekko-group-chat`](ekko-group-chat)

### 5.1 `knowledge_document`

用途：保存知识文档元数据、OSS对象键、处理状态和分片数量。以下结构有当前 DDL 证据。

| 字段 | MySQL定义 | 约束/默认值 | 含义 |
|---|---|---|---|
| `id` | `BIGINT UNSIGNED` | 主键、自增 | 文档ID |
| `title` | `VARCHAR(200)` | 非空 | 展示标题 |
| `file_name` | `VARCHAR(255)` | 非空 | 原始文件名 |
| `content_type` | `VARCHAR(100)` | 非空 | MIME类型 |
| `file_size` | `BIGINT UNSIGNED` | 非空 | 文件字节数 |
| `sha256` | `CHAR(64)` | 非空、唯一索引 | 文件内容摘要/去重依据 |
| `original_object_key` | `VARCHAR(500)` | 可空 | OSS原始文件键 |
| `parsed_object_key` | `VARCHAR(500)` | 可空 | OSS Markdown键 |
| `product_model` | `VARCHAR(100)` | 非空、默认空串 | 产品型号 |
| `status` | `VARCHAR(32)` | 非空 | 处理状态 |
| `chunk_count` | `INT UNSIGNED` | 非空、默认0 | 已写入ES的分片数 |
| `failure_reason` | `VARCHAR(1000)` | 可空 | 最近失败原因 |
| `created_at` | `DATETIME(3)` | 当前时间 | 创建时间 |
| `updated_at` | `DATETIME(3)` | 自动更新时间 | 更新时间 |

索引：

- `PRIMARY KEY (id)`
- `UNIQUE uk_knowledge_document_sha256 (sha256)`
- `idx_knowledge_document_status_updated (status, updated_at)`
- `idx_knowledge_document_product_model (product_model)`

状态流转：`UPLOADING → PARSING → INDEXING → PUBLISHED`，任一步失败进入 `FAILED`。删除文档时当前实现先删 ES 分片，再删 MySQL 元数据，OSS原文件和Markdown刻意保留。  
证据：[`schema.sql`](ekko-group-chat/src/main/resources/db/schema.sql)、[`KnowledgeDocument.java`](ekko-group-chat/src/main/java/cn/ekko/groupchat/document/domain/KnowledgeDocument.java)、[`DocumentIngestionService.java`](ekko-group-chat/src/main/java/cn/ekko/groupchat/document/service/DocumentIngestionService.java)

### 5.2 `admin_user`

用途：保存知识库管理端账号。密码只保存 BCrypt 摘要。

| 字段 | MySQL定义 | 约束/默认值 | 含义 |
|---|---|---|---|
| `id` | `BIGINT UNSIGNED` | 主键、自增 | 管理员ID |
| `username` | `VARCHAR(64)` | 非空、唯一 | 登录账号 |
| `password_hash` | `VARCHAR(100)` | 非空 | BCrypt密码摘要 |
| `enabled` | `TINYINT(1)` | 非空、默认1 | `1`启用、`0`停用 |
| `created_at` | `DATETIME(3)` | 当前时间 | 创建时间 |
| `updated_at` | `DATETIME(3)` | 自动更新时间 | 更新时间 |

索引：`PRIMARY KEY (id)`、`UNIQUE uk_admin_user_username (username)`。  
安全说明：DDL中包含开发用初始管理员数据，生产部署前必须替换初始凭据，文档不复制明文初始密码或哈希。  
证据：[`schema.sql`](ekko-group-chat/src/main/resources/db/schema.sql)、[`AdminUser.java`](ekko-group-chat/src/main/java/cn/ekko/groupchat/auth/domain/AdminUser.java)

---

## 6. 表关系与跨服务关联

当前源码没有提供可确认的外键DDL，下面是业务关联，不代表真实库已创建外键：

```text
拼团营销：
sku.goods_id
   └─ sc_sku_activity.goods_id ──> group_buy_activity.activity_id
                                      ├─ discount_id ──> group_buy_discount.discount_id
                                      └─ tag_id ──> crowd_tags.tag_id

group_buy_order.team_id
   └─ group_buy_order_list.team_id
          └─ out_trade_no ──────────> 支付商城 pay_order.order_id

支付商城：
product.product_id
   └─ pay_order.product_id

RAG客服：
knowledge_document.id
   └─ Elasticsearch分片 metadata.documentId
```

## 7. 非关系型存储（不是MySQL表）

### 7.1 Elasticsearch知识索引

- 默认索引名：`group_chat_knowledge_chunk`
- 分片ID：`doc-{documentId}-chunk-{chunkIndex}`
- 保存内容：文本分片、Embedding向量和元数据
- 元数据字段：`documentId`、`title`、`productModel`、`sourceObjectKey`、`chunkIndex`
- 默认Embedding维度：1024
- 删除方式：按 `metadata.documentId` 删除该文档全部分片

证据：[`application.yml`](ekko-group-chat/src/main/resources/application.yml)、[`ElasticsearchKnowledgeIndex.java`](ekko-group-chat/src/main/java/cn/ekko/groupchat/document/infrastructure/ElasticsearchKnowledgeIndex.java)

### 7.2 OSS对象

- 原文件默认前缀：`knowledge/original/{documentId}/...`
- Markdown默认前缀：`knowledge/parsed/{documentId}/...`
- 删除知识文档接口当前不会删除OSS对象。

### 7.3 Redis、Guava Cache和RabbitMQ

这些属于缓存、分布式锁、Topic/Queue，不属于数据库表。相关定时任务、消息队列和回调契约统一记录在根目录的《接口文档.md》中。

## 8. 上线前必须复核项

1. 对12张无DDL表执行 `SHOW CREATE TABLE` 和 `SHOW INDEX`，补齐真实类型、长度、默认值和约束。
2. 重点确认 `pay_order.order_id`、`product.product_id`、`group_buy_order.team_id`、`group_buy_order_list.out_trade_no`、`notify_task.uuid` 的唯一性/索引。
3. 确认金额列的 `DECIMAL(p,s)` 精度，禁止使用浮点类型。
4. 确认所有状态列能容纳当前枚举，并检查历史数据是否存在未知状态。
5. 确认 `group_buy_activity` 真实存在 `source`、`channel`；当前SQL引用它们，但PO和ResultMap没有声明。
6. `schema.sql` 当前配置不是自动执行的可靠证明，部署时应使用受版本管理的迁移工具或人工审核后的DDL。
